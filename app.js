const seed = {
  projectName: "2910 FED - EXTERIOR TRIM",
  tasks: [
    { component: "a) BUMPER MANAGEMENT", description: "SOURCING", start: "2026-04-01", end: "2026-10-15", status: "Delayed", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "a) BUMPER MANAGEMENT", description: "TOOLING", start: "2026-10-15", end: "2027-03-15", status: "Delayed", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "a) BUMPER MANAGEMENT", description: "TRY OUT", start: "2027-03-15", end: "2027-05-15", status: "Delayed", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "Concluded with a delay", level: 1 },
    { component: "a) BUMPER MANAGEMENT", description: "OTOP", start: "2027-05-15", end: "2027-08-01", status: "Delayed", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "a) BUMPER MANAGEMENT", description: "AQ", start: "2027-08-01", end: "2027-12-01", status: "Delayed", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "CONCEPT", start: "2026-04-01", end: "2026-06-13", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "CAD 1", start: "2026-06-13", end: "2026-08-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "GST", start: "2026-08-08", end: "2026-09-07", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "SOURCING", start: "2026-09-07", end: "2026-12-14", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "CAD 2", start: "2026-08-08", end: "2026-10-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "CAD 3", start: "2026-10-31", end: "2027-01-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "TOOLING", start: "2027-01-29", end: "2027-06-25", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "OT", start: "2027-06-25", end: "2027-08-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "GRAIN", start: "2027-08-24", end: "2027-09-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "BO", start: "2027-09-23", end: "2027-10-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "OTOP", start: "2027-10-23", end: "2027-11-22", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "DV", start: "2027-07-09", end: "2027-10-07", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", description: "PV", start: "2027-11-22", end: "2028-02-20", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "CONCEPT", start: "2026-04-01", end: "2026-07-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "CAD 1", start: "2026-07-24", end: "2026-09-22", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "GST", start: "2026-09-22", end: "2026-10-22", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "SOURCING", start: "2026-10-22", end: "2027-01-28", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "CAD 2", start: "2026-09-22", end: "2026-12-06", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "CAD 3", start: "2026-12-06", end: "2027-03-06", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "TOOLING", start: "2027-03-06", end: "2027-07-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "OT", start: "2027-07-31", end: "2027-09-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "GRAIN", start: "2027-09-29", end: "2027-10-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "BO", start: "2027-10-29", end: "2027-11-28", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "OTOP", start: "2027-11-28", end: "2027-12-28", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "DV", start: "2027-08-14", end: "2027-11-12", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "c) TRADESMAN/REBEL FRONT FASCIA", description: "PV", start: "2027-12-28", end: "2028-03-27", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "GST", start: "2026-04-15", end: "2026-05-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "SOURCING", start: "2026-05-15", end: "2026-08-28", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "CODESIGN", start: "2026-08-28", end: "2027-01-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "TOOLING", start: "2027-01-18", end: "2027-07-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "OT", start: "2027-07-16", end: "2027-10-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "BO", start: "2027-10-02", end: "2027-10-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "OTOP", start: "2027-10-23", end: "2028-01-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "d) AIR SEALING", description: "DV", start: "2027-07-16", end: "2027-09-14", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "d) AIR SEALING", description: "PV", start: "2028-01-08", end: "2028-03-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "GST", start: "2026-04-15", end: "2026-05-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "SOURCING", start: "2026-05-15", end: "2026-09-11", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "DEVELOPMENT", start: "2026-09-11", end: "2026-10-11", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "TOOLING", start: "2026-10-11", end: "2027-03-10", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "OT", start: "2027-03-10", end: "2027-05-09", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "GRAIN", start: "2027-05-09", end: "2027-06-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "BO", start: "2027-06-08", end: "2027-07-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "OTOP", start: "2027-07-08", end: "2027-08-07", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "DV", start: "2027-03-10", end: "2027-06-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "e) BED RAIL COVERS (BOX TRIM)", description: "PV", start: "2027-08-07", end: "2027-11-05", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "GST", start: "2026-03-30", end: "2026-04-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "SOURCING", start: "2026-04-29", end: "2026-08-05", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "DEVELOPMENT", start: "2026-08-05", end: "2026-11-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "TOOLING", start: "2026-11-03", end: "2027-05-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "OT", start: "2027-05-02", end: "2027-07-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "OTOP", start: "2027-07-01", end: "2027-07-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "f) BED LINER + RUBBER COVER", description: "DV", start: "2027-05-02", end: "2027-07-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "f) BED LINER + RUBBER COVER", description: "PV", start: "2027-07-31", end: "2027-10-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "GST", start: "2026-04-30", end: "2026-05-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "SOURCING", start: "2026-05-30", end: "2026-09-19", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "DEVELOPMENT", start: "2026-09-19", end: "2026-10-19", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "TOOLING", start: "2026-10-19", end: "2027-03-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "OT", start: "2027-03-18", end: "2027-05-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "GRAIN", start: "2027-05-17", end: "2027-06-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "BO", start: "2027-06-16", end: "2027-07-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "OTOP", start: "2027-07-16", end: "2027-08-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "g) ROOF MOLDING", description: "DV", start: "2027-03-18", end: "2027-06-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "g) ROOF MOLDING", description: "PV", start: "2027-08-15", end: "2027-11-13", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "GST", start: "2026-04-17", end: "2026-05-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "SOURCING", start: "2026-05-17", end: "2026-08-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "DEVELOPMENT", start: "2026-08-30", end: "2026-12-28", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "TOOLING", start: "2026-12-28", end: "2027-06-26", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "OT", start: "2027-06-26", end: "2027-08-25", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "GRAIN", start: "2027-08-25", end: "2027-09-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "BO", start: "2027-09-24", end: "2027-10-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "OTOP", start: "2027-10-24", end: "2027-12-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "h)  RR BUMPER + BRACKETS", description: "DV", start: "2027-06-26", end: "2027-09-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "h)  RR BUMPER + BRACKETS", description: "PV", start: "2027-12-23", end: "2028-03-22", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "GST", start: "2026-04-30", end: "2026-05-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "SOURCING", start: "2026-05-30", end: "2026-10-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "DEVELOPMENT", start: "2026-10-03", end: "2026-11-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "TOOLING", start: "2026-11-02", end: "2027-04-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "OT", start: "2027-04-01", end: "2027-05-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "GRAIN", start: "2027-05-31", end: "2027-06-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "BO", start: "2027-06-30", end: "2027-07-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "OTOP", start: "2027-07-30", end: "2027-08-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "DV", start: "2027-04-01", end: "2027-06-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "i) DOOR MOLDING (P & G)", description: "PV", start: "2027-08-29", end: "2027-11-27", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "i) DOOR MOLDING (P & G)", description: "WD DESIGN", start: "2026-10-03", end: "2026-12-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "i) DOOR MOLDING (P & G)", description: "WD TOOLING", start: "2026-12-02", end: "2027-05-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "i) DOOR MOLDING (P & G)", description: "WD SHIP", start: "2027-05-01", end: "2027-06-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "j) SILL CLADDING (P & G)", description: "GST", start: "2026-04-30", end: "2026-05-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "SOURCING", start: "2026-05-30", end: "2026-10-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "DEVELOPMENT", start: "2026-10-03", end: "2026-11-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "TOOLING", start: "2026-11-02", end: "2027-04-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "OT", start: "2027-04-01", end: "2027-05-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "GRAIN", start: "2027-05-31", end: "2027-06-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "BO", start: "2027-06-30", end: "2027-07-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "OTOP", start: "2027-07-30", end: "2027-08-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "DV", start: "2027-04-01", end: "2027-06-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "j) SILL CLADDING (P & G)", description: "PV", start: "2027-08-29", end: "2027-11-27", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "j) SILL CLADDING (P & G)", description: "WD DESIGN", start: "2026-10-03", end: "2026-12-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "j) SILL CLADDING (P & G)", description: "WD TOOLING", start: "2026-12-02", end: "2027-05-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "j) SILL CLADDING (P & G)", description: "WD SHIP", start: "2027-05-01", end: "2027-06-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "k) WHEEL FLARE (P & G)", description: "GST", start: "2026-04-30", end: "2026-05-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "SOURCING", start: "2026-05-30", end: "2026-10-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "DEVELOPMENT", start: "2026-10-03", end: "2026-11-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "TOOLING", start: "2026-11-02", end: "2027-04-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "OT", start: "2027-04-01", end: "2027-05-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "GRAIN", start: "2027-05-31", end: "2027-06-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "BO", start: "2027-06-30", end: "2027-07-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "OTOP", start: "2027-07-30", end: "2027-08-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "k) WHEEL FLARE (P & G)", description: "DV", start: "2027-04-01", end: "2027-06-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "k) WHEEL FLARE (P & G)", description: "PV", start: "2027-08-29", end: "2027-11-27", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "k) WHEEL FLARE (P & G)", description: "WD DESIGN", start: "2026-10-03", end: "2026-12-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "k) WHEEL FLARE (P & G)", description: "WD TOOLING", start: "2026-12-02", end: "2027-05-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "k) WHEEL FLARE (P & G)", description: "WD SHIP", start: "2027-05-01", end: "2027-06-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 3 },
    { component: "l) WHEEL LINER", description: "GST", start: "2026-05-01", end: "2026-05-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "SOURCING", start: "2026-05-31", end: "2026-10-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "DEVELOPMENT", start: "2026-10-18", end: "2026-11-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "TOOLING", start: "2026-11-17", end: "2027-04-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "OT", start: "2027-04-16", end: "2027-06-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "BO", start: "2027-06-15", end: "2027-07-20", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "OTOP", start: "2027-07-20", end: "2027-09-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "l) WHEEL LINER", description: "DV", start: "2027-05-01", end: "2027-07-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "l) WHEEL LINER", description: "PV", start: "2027-09-18", end: "2027-12-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "m) CROSSMEMBER REINF", description: "GST", start: "2026-05-01", end: "2026-05-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "SOURCING", start: "2026-05-31", end: "2026-10-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "DEVELOPMENT", start: "2026-10-18", end: "2026-11-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "TOOLING", start: "2026-11-17", end: "2027-04-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "OT", start: "2027-04-16", end: "2027-06-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "BO", start: "2027-06-15", end: "2027-07-20", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "OTOP", start: "2027-07-20", end: "2027-09-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "m) CROSSMEMBER REINF", description: "DV", start: "2027-05-01", end: "2027-07-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "m) CROSSMEMBER REINF", description: "PV", start: "2027-09-18", end: "2027-12-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "n) WHEEL LINER UPPER COVER", description: "GST", start: "2026-05-01", end: "2026-05-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "SOURCING", start: "2026-05-31", end: "2026-10-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "DEVELOPMENT", start: "2026-10-18", end: "2026-11-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "TOOLING", start: "2026-11-17", end: "2027-04-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "OT", start: "2027-04-16", end: "2027-06-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "BO", start: "2027-06-15", end: "2027-07-20", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "OTOP", start: "2027-07-20", end: "2027-09-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "n) WHEEL LINER UPPER COVER", description: "DV", start: "2027-05-01", end: "2027-07-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "n) WHEEL LINER UPPER COVER", description: "PV", start: "2027-09-18", end: "2027-12-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "o) PANEL CLOSE OUT", description: "GST", start: "2026-04-17", end: "2026-05-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "SOURCING", start: "2026-05-17", end: "2026-09-06", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "DEVELOPMENT", start: "2026-09-06", end: "2026-10-06", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "TOOLING", start: "2026-10-06", end: "2027-03-05", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "OT", start: "2027-03-05", end: "2027-05-04", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "GRAIN", start: "2027-05-04", end: "2027-06-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "BO", start: "2027-06-03", end: "2027-07-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "OTOP", start: "2027-07-03", end: "2027-08-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "o) PANEL CLOSE OUT", description: "DV", start: "2027-03-05", end: "2027-06-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "o) PANEL CLOSE OUT", description: "PV", start: "2027-08-02", end: "2027-10-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "p) TIRE SPATS", description: "GST", start: "2026-04-24", end: "2026-05-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "SOURCING", start: "2026-05-24", end: "2026-10-11", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "DEVELOPMENT", start: "2026-10-11", end: "2026-11-10", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "TOOLING", start: "2026-11-10", end: "2027-03-25", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "OT", start: "2027-03-25", end: "2027-05-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "GRAIN", start: "2027-05-24", end: "2027-06-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "BO", start: "2027-06-23", end: "2027-07-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "OTOP", start: "2027-07-23", end: "2027-08-22", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "p) TIRE SPATS", description: "DV", start: "2027-03-25", end: "2027-06-23", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "p) TIRE SPATS", description: "PV", start: "2027-08-22", end: "2027-11-20", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "q) AERO SHIELD", description: "GST", start: "2026-04-17", end: "2026-05-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "SOURCING", start: "2026-05-17", end: "2026-10-04", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "DEVELOPMENT", start: "2026-10-04", end: "2026-11-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "TOOLING", start: "2026-11-03", end: "2027-04-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "OT", start: "2027-04-02", end: "2027-06-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "BO", start: "2027-06-01", end: "2027-07-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "OTOP", start: "2027-07-01", end: "2027-07-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "q) AERO SHIELD", description: "DV", start: "2027-04-02", end: "2027-07-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "q) AERO SHIELD", description: "PV", start: "2027-07-31", end: "2027-10-29", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "r) CARGO HOOK (METAL)", description: "GST", start: "2026-04-24", end: "2026-05-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "SOURCING", start: "2026-05-24", end: "2026-11-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "DEVELOPMENT", start: "2026-11-08", end: "2026-12-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "TOOLING", start: "2026-12-08", end: "2027-05-07", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "OT", start: "2027-05-07", end: "2027-07-06", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "BO", start: "2027-07-06", end: "2027-08-05", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "OTOP", start: "2027-08-05", end: "2027-09-04", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "r) CARGO HOOK (METAL)", description: "DV", start: "2027-05-07", end: "2027-08-05", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "r) CARGO HOOK (METAL)", description: "PV", start: "2027-09-04", end: "2027-12-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "GST", start: "2026-04-24", end: "2026-05-24", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "SOURCING", start: "2026-05-24", end: "2026-11-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "DEVELOPMENT", start: "2026-11-08", end: "2026-12-08", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "TOOLING", start: "2026-12-08", end: "2027-04-22", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "OT", start: "2027-04-22", end: "2027-06-21", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "GRAIN", start: "2027-06-21", end: "2027-07-21", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "BO", start: "2027-07-21", end: "2027-08-20", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "OTOP", start: "2027-08-20", end: "2027-09-19", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "DV", start: "2027-04-22", end: "2027-07-21", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "s) CARGO HOOK (PLASTIC)", description: "PV", start: "2027-09-19", end: "2027-12-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "t) WATER SHIELD", description: "GST", start: "2026-05-04", end: "2026-06-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "SOURCING", start: "2026-06-03", end: "2026-11-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "DEVELOPMENT", start: "2026-11-18", end: "2026-12-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "TOOLING", start: "2026-12-18", end: "2027-05-17", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "OT", start: "2027-05-17", end: "2027-07-16", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "BO", start: "2027-07-16", end: "2027-08-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "OTOP", start: "2027-08-15", end: "2027-09-14", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "t) WATER SHIELD", description: "DV", start: "2027-05-17", end: "2027-08-15", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "t) WATER SHIELD", description: "PV", start: "2027-09-14", end: "2027-12-13", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "u) CARGO BED DRAIN", description: "GST", start: "2026-05-04", end: "2026-06-03", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "SOURCING", start: "2026-06-03", end: "2026-11-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "DEVELOPMENT", start: "2026-11-18", end: "2026-12-18", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "TOOLING", start: "2026-12-18", end: "2027-05-02", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "OT", start: "2027-05-02", end: "2027-07-01", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "BO", start: "2027-07-01", end: "2027-07-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "OTOP", start: "2027-07-31", end: "2027-08-30", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 1 },
    { component: "u) CARGO BED DRAIN", description: "DV", start: "2027-05-02", end: "2027-07-31", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 },
    { component: "u) CARGO BED DRAIN", description: "PV", start: "2027-08-30", end: "2027-11-28", status: "On time", progress: 0, milestone: "", milestoneDate: "", milestoneStatus: "", level: 2 }
  ].map(({ component, description, start, end, status, progress, milestone, milestoneDate, milestoneStatus, level }, index) => ({
    id: crypto.randomUUID(), index: index + 1,
    component, description, start, end, status, progress,
    milestone, milestoneDate, milestoneStatus, level,
    baselines: [{ id: "v1", start, end }],
  })),
  milestones: [
    { id: crypto.randomUUID(), name: "IM", type: "Main Milestone", start: "2026-01-15", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "PM", type: "Main Milestone", start: "2026-03-10", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "CM", type: "Main Milestone", start: "2026-05-11", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "SFM", type: "Main Milestone", start: "2026-05-29", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "SHRM", type: "Main Milestone", start: "2026-11-13", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X0", type: "Main Milestone", start: "2027-07-16", end: "", level: 1, color: 3, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X0", type: "Main Milestone", start: "2027-08-16", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X1", type: "Main Milestone", start: "2027-10-01", end: "", level: 1, color: 3, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X1", type: "Main Milestone", start: "2027-11-01", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X2", type: "Main Milestone", start: "2027-12-17", end: "", level: 1, color: 3, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X2", type: "Main Milestone", start: "2028-01-17", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "LRM", type: "Main Milestone", start: "2028-02-21", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "X3", type: "Main Milestone", start: "2028-02-28", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "MPRM", type: "Main Milestone", start: "2027-10-25", end: "", level: 1, color: 0, component: "", description: "" },
    { id: crypto.randomUUID(), name: "SOP", type: "Main Milestone", start: "2028-03-27", end: "", level: 1, color: 3, component: "", description: "" },
    { id: crypto.randomUUID(), name: "SOP", type: "Main Milestone", start: "2028-04-28", end: "", level: 1, color: 0, component: "", description: "" }
  ],
  freeMilestones: [
    { id: crypto.randomUUID(), name: "1C", status: "Concluded", date: "2026-05-22", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "2C", status: "Ontime", date: "2026-06-15", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "1B", status: "Ontime", date: "2026-07-06", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "2B", status: "Ontime", date: "2026-08-03", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "3B", status: "Ontime", date: "2026-09-07", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "1A", status: "Ontime", date: "2026-10-05", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "2A", status: "Ontime", date: "2026-11-02", component: "c) TRADESMAN/REBEL FRONT FASCIA", level: 1 },
    { id: crypto.randomUUID(), name: "C", status: "Concluded", date: "2026-05-18", component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", level: 1 },
    { id: crypto.randomUUID(), name: "CB", status: "Ontime", date: "2026-06-22", component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", level: 1 },
    { id: crypto.randomUUID(), name: "B", status: "Ontime", date: "2026-07-20", component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", level: 1 },
    { id: crypto.randomUUID(), name: "A", status: "Ontime", date: "2026-08-17", component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", level: 1 },
    { id: crypto.randomUUID(), name: "A", status: "Ontime", date: "2026-09-14", component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", level: 1 },
    { id: crypto.randomUUID(), name: "A", status: "Ontime", date: "2026-10-13", component: "b) GRILL WITH CAMERA + FASCIA WITH SIDE MARK", level: 1 }
  ],
  dependencies: []
};

const statusMeta = {
  "On time": { color: "#4a76b8", soft: "#dbe8f8", text: "#17365d" },
  "Attention point": { color: "#b87900", soft: "#fff1c7", text: "#5d3b00" },
  Delayed: { color: "#c74444", soft: "#f8dddd", text: "#681a1a" },
  Concluded: { color: "#4a5aa6", soft: "#e6eaff", text: "#1d285f" },
  "Concluded with a delay": { color: "#8d4f2b", soft: "#f3dfd3", text: "#4d2411" },
  "Concluded Delay": { color: "#8d4f2b", soft: "#f3dfd3", text: "#4d2411" },
  Ontime: { color: "#4a76b8", soft: "#dbe8f8", text: "#17365d" },
  "": { color: "#6f7885", soft: "#e7ebf0", text: "#303640" },
};

// Escurece uma cor hex (fator 0-1, onde 0 = preto)
function darkenHex(hex, factor = 0.72) {
  const m = hex.match(/\w\w/g);
  if (!m) return "#444";
  const [r, g, b] = m.map(c => Math.round(parseInt(c, 16) * factor));
  return `#${[r, g, b].map(c => c.toString(16).padStart(2, "0")).join("")}`;
}

const appShell = document.querySelector(".app-shell");

// Sidebar inicia RECOLHIDA por padrão (Etapa 1)
// Só expande se o usuário tiver salvo explicitamente "expanded"
const savedSidebar = localStorage.getItem("sidebar");
if (savedSidebar !== "expanded") {
  appShell.classList.add("sidebar-collapsed");
}

toggleSidebarButton.addEventListener("click", () => {
  appShell.classList.toggle("sidebar-collapsed");
  const isCollapsed = appShell.classList.contains("sidebar-collapsed");
  localStorage.setItem("sidebar", isCollapsed ? "collapsed" : "expanded");

  // Atualiza o title do botão para acessibilidade
  toggleSidebarButton.title = isCollapsed ? "Expandir menu" : "Recolher menu";
});

function getActiveProject() {
  return state.projects?.find(p => p.id === state.activeProjectId)
    || state.projects?.[0]
    || null;
}

// Copia dados do projeto ativo → aliases no root do state
function syncFromActiveProject(s = state) {
  const p = s.projects?.find(proj => proj.id === s.activeProjectId)
    || s.projects?.[0];
  if (!p) return;
  s.tasks          = p.tasks          ?? [];
  s.milestones     = p.milestones     ?? [];
  s.freeMilestones = p.freeMilestones ?? [];
  s.dependencies   = p.dependencies   ?? [];
  s.projectName    = p.name           ?? "";
}

// Copia aliases do root → projeto ativo (chamado antes de salvar)
function syncToActiveProject() {
  const p = getActiveProject();
  if (!p) return;
  p.tasks          = state.tasks;
  p.milestones     = state.milestones;
  p.freeMilestones = state.freeMilestones;
  p.dependencies   = state.dependencies;
  p.name           = state.projectName;
}

// Normaliza e migra qualquer formato de estado salvo
function normalizeState(raw) {
  // Migrar formato pré-Etapa 4 (sem projects[])
  if (!raw.projects) {
    const id = crypto.randomUUID();
    raw = {
      activeProjectId: id,
      projects: [{
        id,
        name:        raw.projectName || "Projeto 1",
        client:      "",
        manager:     "",
        startDate:   "",
        endDate:     "",
        status:      "Active",
        createdAt:   iso(new Date()),
        tasks:          raw.tasks          || [],
        milestones:     raw.milestones     || [],
        freeMilestones: raw.freeMilestones || [],
        dependencies:   raw.dependencies   || []
      }]
    };
  }

  // Garante activeProjectId válido
  if (!raw.projects.find(p => p.id === raw.activeProjectId)) {
    raw.activeProjectId = raw.projects[0]?.id;
  }

  // Garante campos obrigatórios em cada projeto
  raw.projects.forEach(p => {
    p.tasks          ??= [];
    p.milestones     ??= [];
    p.freeMilestones ??= [];
    p.dependencies   ??= [];
    p.team ??= [];
    p.status         ??= "Active";
    p.client         ??= "";
    p.manager        ??= "";
    p.startDate      ??= "";
    p.endDate        ??= "";
    p.createdAt      ??= iso(new Date());

    // Baseline migration
    p.tasks.forEach(task => {
      task.baselines ??= [];
      if (!task.baselines.length) {
        task.baselines.push({ id: "v1", start: task.start, end: task.end });
      }
    });

    // Milestone component/description fields migration
    p.milestones.forEach(m => {
      m.component   ??= "";
      m.description ??= "";
    });
  });

  raw.users        ??= [];   // ← NOVO
  raw.projectUsers ??= [];   // ← NOVO

  syncFromActiveProject(raw);
  return raw;
}


const storageKey = "schedule-studio-state-v1";
let state = loadState();
let zoom = "month";
let compactZoom = "month";
let compactLabelMode = "weeks";
let activeBaselineId = "v1";
let compactLabelColWidth = 260;   // largura da coluna de componentes em px
let compactMonthScale = 1.0; 

const zoomConfig = {
  week: {
    pxPerDay: 9,
    label: "Week"
  },

  month: {
    pxPerDay: 4.2,
    label: "Month"
  },

  quarter: {
    pxPerDay: 2,
    label: "Quarter"
  }
};
let showDependencies = true;
let collapsedComponents = {};
let sortState = { field: null, dir: 'asc' };
let selectedTaskIds = new Set();
const history = {
  undoStack: [],
  redoStack: [],
};

function cloneState() {
  syncToActiveProject();
  const snapshot = JSON.parse(JSON.stringify({
    activeProjectId: state.activeProjectId,
    users:           state.users        ?? [],
    projectUsers:    state.projectUsers ?? [],
    projects:        state.projects
  }));
  syncFromActiveProject(snapshot);
  return snapshot
}

function renderBaselineSelector() {
  const select = document.getElementById("baselineSelector");

  if (!select) return;

  const baselines = state.tasks[0]?.baselines || [];

  select.innerHTML = baselines
    .map(
      (b, index) =>
        `<option value="${b.id}">
          Baseline ${index + 1} (${b.start} → ${b.end})
        </option>`
    )
    .join("");

  select.value = activeBaselineId;
}

function renderProjectSelector() {
  const sel = document.getElementById("projectSelector");
  if (!sel) return;

  const active   = state.projects.filter(p => p.status !== "Archived");
  const archived = state.projects.filter(p => p.status === "Archived");

  let html = active
    .map(p => `<option value="${p.id}" ${p.id === state.activeProjectId ? "selected" : ""}>${escapeHtml(p.name)}</option>`)
    .join("");

  if (archived.length) {
    html += `<optgroup label="Arquivados">${
      archived.map(p => `<option value="${p.id}" ${p.id === state.activeProjectId ? "selected" : ""}>${escapeHtml(p.name)}</option>`).join("")
    }</optgroup>`;
  }

  sel.innerHTML = html;
}

function commitState() {
  history.undoStack.push(cloneState());

  if (history.undoStack.length > 50) {
    history.undoStack.shift();
  }

  history.redoStack = [];
}

let _renderDebounceTimer = null;
let _commitDebounced     = false;

// Debounced commit: creates at most one undo point per 800 ms burst of edits.
function commitStateDebounced() {
  if (_commitDebounced) return;
  commitState();
  _commitDebounced = true;
  setTimeout(() => { _commitDebounced = false; }, 800);
}

// Defers renderAll() by 600 ms so rapid field changes (e.g. typing a year in
// a date input) don't destroy the active DOM element mid-edit.
function scheduleRender() {
  clearTimeout(_renderDebounceTimer);
  _renderDebounceTimer = setTimeout(() => {
    _renderDebounceTimer = null;
    renderAll();
  }, 600);
}

function undo() {
  if (!history.undoStack.length) return;
  history.redoStack.push(cloneState());
  state = history.undoStack.pop();
  syncFromActiveProject();
  saveState();
  renderAll();
}

function redo() {
  if (!history.redoStack.length) return;
  history.undoStack.push(cloneState());
  state = history.redoStack.pop();
  syncFromActiveProject();
  saveState();
  renderAll();
}

function switchProject(id) {
  if (!id || id === state.activeProjectId) return;
  commitState();

  const loader = document.getElementById("mainLoader");
  if (loader) loader.hidden = false;

  syncToActiveProject();
  state.activeProjectId = id;
  syncFromActiveProject();
  saveState();
  renderAll();

  if (loader) loader.hidden = true;

  const p = getActiveProject();
  showToast(`Projeto "${p?.name}" ativado`, "info");
}

function createProject(data) {
  commitState();
  syncToActiveProject();

  const id = crypto.randomUUID();
  const newProject = {
    id,
    name:        data.name      || "Novo Projeto",
    client:      data.client    || "",
    manager:     data.manager   || "",
    startDate:   data.startDate || "",
    endDate:     data.endDate   || "",
    status:      "Active",
    createdAt:   iso(new Date()),
    team: [],
    tasks:          [],
    milestones:     [],
    freeMilestones: [],
    dependencies:   []
  };

  state.projects.push(newProject);
  state.activeProjectId = id;
  syncFromActiveProject();
  saveState();
  renderAll();
  showToast(`Projeto "${newProject.name}" criado`);
}

function updateProject(id, data) {
  commitState();
  const p = state.projects.find(proj => proj.id === id);
  if (!p) return;

  Object.assign(p, data);

  // Se é o projeto ativo, atualiza o alias
  if (id === state.activeProjectId) {
    state.projectName = p.name;
  }

  saveState();
  renderAll();
  showToast("Projeto atualizado");
}

function archiveProject(id) {
  if (!confirm("Arquivar este projeto? Ele não será excluído.")) return;
  commitState();

  const p = state.projects.find(proj => proj.id === id);
  if (!p) return;

  p.status = "Archived";

  // Se arquivou o projeto ativo, muda para o próximo disponível
  if (id === state.activeProjectId) {
    const next = state.projects.find(proj => proj.id !== id && proj.status !== "Archived");
    if (next) {
      state.activeProjectId = next.id;
      syncFromActiveProject();
    }
  }

  saveState();
  renderAll();
  showToast(`Projeto "${p.name}" arquivado`, "warning")
}

function duplicateProject(id) {
  commitState();
  syncToActiveProject();

  const source = state.projects.find(p => p.id === id);
  if (!source) return;

  const newId = crypto.randomUUID();
  const copy  = JSON.parse(JSON.stringify(source));

  copy.id        = newId;
  copy.name      = `${source.name} (cópia)`;
  copy.status    = "Active";
  copy.createdAt = iso(new Date());

  // Remapeia IDs das tasks para evitar colisões
  copy.tasks          = copy.tasks.map(t  => ({ ...t,  id: crypto.randomUUID() }));
  copy.milestones     = copy.milestones.map(m => ({ ...m, id: crypto.randomUUID() }));
  copy.freeMilestones = copy.freeMilestones.map(m => ({ ...m, id: crypto.randomUUID() }));
  copy.team         = (copy.team || []).map(m => ({ ...m, id: crypto.randomUUID() }));
  copy.dependencies = []; // IDs de tasks mudaram — resetar

  state.projects.push(copy);
  state.activeProjectId = newId;
  syncFromActiveProject();
  saveState();
  renderAll();
  showToast(`Projeto duplicado: "${copy.name}"`);
}

function renderSettingsView() {
  const nameEl = document.getElementById("settingsProjectName");
  if (nameEl) nameEl.textContent = getActiveProject()?.name || "—";

  renderUsersTable();
  renderProjectUsersTable();
  populateAddProjectUserSelect();
}

function renderUsersTable() {
  const el = document.getElementById("usersTable");
  if (!el) return;

  const perfilMeta = {
    "Administrador": { color: "var(--red)",    soft: "var(--red-soft)"    },
    "Gerente":       { color: "var(--teal)",   soft: "var(--teal-soft)"   },
    "Analista":      { color: "var(--indigo)", soft: "var(--indigo-soft)" },
  };

  const users = state.users ?? [];

  if (!users.length) {
    el.innerHTML = `
      <thead><tr><th>Nome</th><th>Email</th><th>Perfil</th><th></th></tr></thead>
      <tbody><tr><td colspan="4">
        <div class="empty-state" style="padding:24px">Nenhum usuário cadastrado.</div>
      </td></tr></tbody>`;
    return;
  }

  const rows = users.map(u => {
    const meta = perfilMeta[u.perfil] || perfilMeta["Analista"];
    return `<tr>
      <td><strong>${escapeHtml(u.nome)}</strong></td>
      <td style="color:var(--muted);font-size:13px">${escapeHtml(u.email || "—")}</td>
      <td>
        <span class="auth-badge" style="background:${meta.soft};color:${meta.color}">
          ${escapeHtml(u.perfil)}
        </span>
      </td>
      <td>
        <div class="project-row-actions" style="opacity:1">
          <button class="row-action" data-edit-user="${u.id}" title="Editar">
            <i data-lucide="pencil"></i>
          </button>
          <button class="row-action" data-delete-user="${u.id}" title="Remover">
            <i data-lucide="trash-2"></i>
          </button>
        </div>
      </td>
    </tr>`;
  }).join("");

  el.innerHTML = `
    <thead><tr><th>Nome</th><th>Email</th><th>Perfil</th><th></th></tr></thead>
    <tbody>${rows}</tbody>`;
}

function renderProjectUsersTable() {
  const el = document.getElementById("projectUsersTable");
  if (!el) return;

  const proj = getActiveProject();
  if (!proj) return;

  const projectUsers = state.projectUsers ?? [];
  const linked = projectUsers.filter(pu => pu.projectId === proj.id);

  const perfilMeta = {
    "Gerente":  { color: "var(--teal)",   soft: "var(--teal-soft)"   },
    "Analista": { color: "var(--indigo)", soft: "var(--indigo-soft)" },
  };

  if (!linked.length) {
    el.innerHTML = `
      <thead><tr><th>Usuário</th><th>Email</th><th>Função</th><th></th></tr></thead>
      <tbody><tr><td colspan="4">
        <div class="empty-state" style="padding:24px">Nenhum usuário vinculado a este projeto.</div>
      </td></tr></tbody>`;
    return;
  }

  const rows = linked.map(pu => {
    const user = (state.users ?? []).find(u => u.id === pu.userId);
    if (!user) return "";
    const meta = perfilMeta[pu.funcao] || perfilMeta["Analista"];
    return `<tr>
      <td><strong>${escapeHtml(user.nome)}</strong></td>
      <td style="color:var(--muted);font-size:13px">${escapeHtml(user.email || "—")}</td>
      <td>
        <span class="auth-badge" style="background:${meta.soft};color:${meta.color}">
          ${escapeHtml(pu.funcao)}
        </span>
      </td>
      <td>
        <div class="project-row-actions" style="opacity:1">
          <button class="row-action" data-remove-project-user="${pu.id}" title="Desvincular">
            <i data-lucide="link-2-off"></i>
          </button>
        </div>
      </td>
    </tr>`;
  }).join("");

  el.innerHTML = `
    <thead><tr><th>Usuário</th><th>Email</th><th>Função</th><th></th></tr></thead>
    <tbody>${rows}</tbody>`;
}

function populateAddProjectUserSelect() {
  const sel = document.getElementById("addProjectUserSelect");
  if (!sel) return;

  const proj = getActiveProject();
  const linkedIds = new Set(
    (state.projectUsers ?? [])
      .filter(pu => pu.projectId === proj?.id)
      .map(pu => pu.userId)
  );

  const available = (state.users ?? []).filter(u => !linkedIds.has(u.id));

  sel.innerHTML = available.length
    ? `<option value="">Selecionar usuário...</option>` +
      available.map(u =>
        `<option value="${u.id}">${escapeHtml(u.nome)} — ${escapeHtml(u.perfil)}</option>`
      ).join("")
    : `<option value="">Nenhum usuário disponível</option>`;
}

// ── CRUD de usuários ─────────────────────────────────────────

function openUserModal(userId = null) {
  const modal   = document.getElementById("userModal");
  const titleEl = document.getElementById("userModalTitle");

  if (userId) {
    const u = (state.users ?? []).find(user => user.id === userId);
    if (!u) return;
    titleEl.textContent = "Editar Usuário";
    document.getElementById("userModalId").value     = u.id;
    document.getElementById("userModalNome").value   = u.nome;
    document.getElementById("userModalEmail").value  = u.email  || "";
    document.getElementById("userModalPerfil").value = u.perfil || "Analista";
  } else {
    titleEl.textContent = "Novo Usuário";
    document.getElementById("userModalId").value     = "";
    document.getElementById("userModalNome").value   = "";
    document.getElementById("userModalEmail").value  = "";
    document.getElementById("userModalPerfil").value = "Analista";
  }

  modal.showModal();
}

function saveUser() {
  const id     = document.getElementById("userModalId").value;
  const nome   = document.getElementById("userModalNome").value.trim();
  const email  = document.getElementById("userModalEmail").value.trim();
  const perfil = document.getElementById("userModalPerfil").value;

  if (!validateRequired("userModalNome")) return;

  commitState();
  state.users ??= [];

  if (id) {
    const u = state.users.find(user => user.id === id);
    if (u) Object.assign(u, { nome, email, perfil });
    showToast("Usuário atualizado");
  } else {
    state.users.push({ id: crypto.randomUUID(), nome, email, perfil });
    showToast(`Usuário "${nome}" criado`);
  }

  saveState();
  renderSettingsView();
  lucide.createIcons();
  document.getElementById("userModal").close();
}

function removeUser(userId) {
  if (!confirm("Remover este usuário? Ele será desvinculado de todos os projetos.")) return;
  commitState();
  state.users        = (state.users        ?? []).filter(u  => u.id  !== userId);
  state.projectUsers = (state.projectUsers ?? []).filter(pu => pu.userId !== userId);
  saveState();
  renderSettingsView();
  lucide.createIcons();
  showToast("Usuário removido", "warning")
}

// ── Vínculos ProjetoUsuário ───────────────────────────────────

function addProjectUser(userId, funcao) {
  if (!userId) { showToast("Selecione um usuário"); return; }

  const proj = getActiveProject();
  if (!proj) return;

  state.projectUsers ??= [];

  const alreadyLinked = state.projectUsers.some(
    pu => pu.projectId === proj.id && pu.userId === userId
  );
  if (alreadyLinked) { showToast("Usuário já vinculado a este projeto"); return; }

  commitState();
  state.projectUsers.push({
    id:        crypto.randomUUID(),
    projectId: proj.id,
    userId,
    funcao
  });

  saveState();
  renderSettingsView();
  lucide.createIcons();

  const user = (state.users ?? []).find(u => u.id === userId);
  showToast(`${user?.nome || "Usuário"} vinculado como ${funcao}`);
}

function removeProjectUser(projectUserId) {
  commitState();
  state.projectUsers = (state.projectUsers ?? []).filter(pu => pu.id !== projectUserId);
  saveState();
  renderSettingsView();
  lucide.createIcons();
  showToast("Vínculo removido");
}


function addTeamMember(name, role) {
  const name_ = name.trim();
  if (!name_) { showToast("Informe o nome do membro"); return; }

  const p = getActiveProject();
  if (!p) return;

  // Impede duplicata de gerente
  if (role === "Gerente" && p.team?.some(m => m.role === "Gerente")) {
    showToast("Este projeto já possui um Gerente. Remova o atual antes de adicionar um novo.");
    return;
  }

  commitState();
  p.team ??= [];
  p.team.push({ id: crypto.randomUUID(), name: name_, role });

  // Se for Gerente, sincroniza o campo manager do projeto
  if (role === "Gerente") p.manager = name_;

  saveState();
  renderTeamView();
  lucide.createIcons();
  showToast(`${name_} adicionado(a) como ${role}`);
}

function removeTeamMember(memberId) {
  const p = getActiveProject();
  if (!p || !p.team) return;

  const member = p.team.find(m => m.id === memberId);
  if (!member) return;

  commitState();
  p.team = p.team.filter(m => m.id !== memberId);

  // Se era o Gerente, limpa o campo manager
  if (member.role === "Gerente") p.manager = "";

  saveState();
  renderTeamView();
  lucide.createIcons();
  showToast(`${member.name} removido(a) da equipe`, "warning")
}


function openProjectModal(projectId = null) {
  const modal   = document.getElementById("projectModal");
  const titleEl = document.getElementById("projectModalTitle");

  if (projectId) {
    const p = state.projects.find(proj => proj.id === projectId);
    if (!p) return;
    titleEl.textContent = "Editar Projeto";
    document.getElementById("projectModalId").value      = p.id;
    document.getElementById("projectModalName").value    = p.name;
    document.getElementById("projectModalClient").value  = p.client   || "";
    document.getElementById("projectModalManager").value = p.manager  || "";
    document.getElementById("projectModalStart").value   = p.startDate || "";
    document.getElementById("projectModalEnd").value     = p.endDate   || "";
    document.getElementById("projectModalStatus").value  = p.status   || "Active";
  } else {
    titleEl.textContent = "Novo Projeto";
    document.getElementById("projectModalId").value      = "";
    document.getElementById("projectModalName").value    = "";
    document.getElementById("projectModalClient").value  = "";
    document.getElementById("projectModalManager").value = "";
    document.getElementById("projectModalStart").value   = "";
    document.getElementById("projectModalEnd").value     = "";
    document.getElementById("projectModalStatus").value  = "Active";
  }

  modal.showModal();
}

const $ = (selector) => document.querySelector(selector);
const $$ = (selector) => [...document.querySelectorAll(selector)];
const dayMs = 24 * 60 * 60 * 1000;

function loadState() {
  const saved = localStorage.getItem(storageKey);
  let raw = null;

  if (saved) {
    try { raw = JSON.parse(saved); } catch { raw = null; }
  }

  if (!raw) {
    // Sem estado salvo — constrói a partir do seed
    const id = crypto.randomUUID();
    raw = {
      activeProjectId: id,
      users:        [],     // ← NOVO
      projectUsers: [],     // ← NOVO
      projects: [{
        id,
        name:        seed.projectName,
        client:      "",
        manager:     "",
        startDate:   "",
        endDate:     "",
        status:      "Active",
        createdAt:   iso(new Date()),
        tasks:          structuredClone(seed.tasks),
        milestones:     structuredClone(seed.milestones),
        freeMilestones: structuredClone(seed.freeMilestones),
        dependencies:   structuredClone(seed.dependencies ?? [])
      }]
    };
    syncFromActiveProject(raw);
    return raw;
  }

  return normalizeState(raw);
}


function captureBaseline() {
  commitState();

  const version = `v${Date.now()}`;

  state.tasks.forEach((task) => {
    task.baselines ??= [];

    task.baselines.push({
      id: version,
      start: task.start,
      end: task.end
    });
  });

  activeBaselineId = version;

  saveState();
  renderAll();

  showToast("New baseline version created");
}

function showSaveIndicator() {
  const el = document.getElementById("saveIndicator");
  if (!el) return;
  el.classList.add("visible");
  clearTimeout(el._timer);
  el._timer = setTimeout(() => el.classList.remove("visible"), 2200);
}


function saveState() {
  syncToActiveProject();
  localStorage.setItem(storageKey, JSON.stringify({
    activeProjectId: state.activeProjectId,
    users:           state.users        ?? [],
    projectUsers:    state.projectUsers ?? [],
    projects:        state.projects
  }));
  showSaveIndicator();
}

function dateValue(value) {
  return value ? new Date(`${value}T00:00:00`) : null;
}

function iso(date) {
  return date.toISOString().slice(0, 10);
}

function formatDate(value) {
  if (!value) return "";
  return new Intl.DateTimeFormat("pt-BR", { day: "2-digit", month: "2-digit", year: "2-digit" }).format(dateValue(value));
}

function monthLabel(date) {
  return new Intl.DateTimeFormat("en", { month: "short", year: "2-digit" }).format(date);
}

function quarterLabel(date) {
  const quarter =
    Math.floor(
      date.getMonth() / 3
    ) + 1;

  return `Q${quarter}`;
}

function getFilteredTasks() {
  const search = $("#searchInput").value.trim().toLowerCase();
  const component = $("#componentFilter").value;
  const status = $("#statusFilter").value;
  return state.tasks.filter((task) => {
    const matchSearch = !search || `${task.component} ${task.description}`.toLowerCase().includes(search);
    const matchComponent = component === "all" || task.component === component;
    const matchStatus = status === "all" || (status === "blank" ? !task.status : task.status === status);
    return matchSearch && matchComponent && matchStatus;
  });
}

function getSortedTasks(tasks) {
  if (!sortState.field) return tasks;
  return [...tasks].sort((a, b) => {
    let va = a[sortState.field] ?? '';
    let vb = b[sortState.field] ?? '';
    if (typeof va === 'string') { va = va.toLowerCase(); vb = String(vb).toLowerCase(); }
    if (va < vb) return sortState.dir === 'asc' ? -1 : 1;
    if (va > vb) return sortState.dir === 'asc' ? 1 : -1;
    return 0;
  });
}

function recalculateStatuses() {
  const today = new Date(); today.setHours(0, 0, 0, 0);
  const soonMs = today.getTime() + 14 * 86400000;
  let changed = 0;
  state.tasks.forEach(task => {
    const pct = Math.round((task.progress ?? 0) * 100);
    const fin = task.end ? new Date(task.end + 'T00:00:00') : null;
    const prev = task.status;
    if (pct >= 100) {
      task.status = fin && fin < today ? "Concluded with a delay" : "Concluded";
    } else if (fin && fin < today) {
      task.status = "Delayed";
    } else if (fin && fin.getTime() <= soonMs) {
      task.status = "Attention point";
    } else if (pct > 0) {
      task.status = "On time";
    }
    if (task.status !== prev) changed++;
  });
  syncToActiveProject(); saveState(); renderAll();
  showToast(`${changed} atividade${changed !== 1 ? 's' : ''} atualizada${changed !== 1 ? 's' : ''}`);
}

function updateBulkBar() {
  const bar = document.getElementById("bulkBar");
  const count = document.getElementById("bulkCount");
  if (!bar || !count) return;
  const n = selectedTaskIds.size;
  bar.style.display = n === 0 ? 'none' : 'flex';
  count.textContent = `${n} selecionada${n !== 1 ? 's' : ''}`;
}

function applyBulkEdit() {
  const status = document.getElementById("bulkStatus")?.value;
  const progressRaw = document.getElementById("bulkProgress")?.value;
  const progress = progressRaw !== "" ? parseFloat(progressRaw) / 100 : null;
  if (!status && progress === null) return;
  commitState();
  state.tasks.forEach(task => {
    if (!selectedTaskIds.has(task.id)) return;
    if (status) task.status = status;
    if (progress !== null && !isNaN(progress)) task.progress = Math.max(0, Math.min(1, progress));
  });
  selectedTaskIds.clear();
  syncToActiveProject(); saveState(); renderAll();
  showToast("Atualização em lote aplicada");
}

function bulkDeleteTasks() {
  const n = selectedTaskIds.size;
  if (!n) return;
  if (!confirm(`Excluir ${n} atividade${n !== 1 ? 's' : ''}?`)) return;
  commitState();
  state.tasks = state.tasks.filter(t => !selectedTaskIds.has(t.id));
  selectedTaskIds.clear();
  syncToActiveProject(); saveState(); renderAll();
}

function getRange(tasks = state.tasks) {
  const dates = [
    ...tasks.flatMap((task) => [dateValue(task.start), dateValue(task.end), dateValue(task.milestoneDate)]),
    ...state.milestones.flatMap((item) => [dateValue(item.start), dateValue(item.end)]),
    ...state.freeMilestones.map((item) => dateValue(item.date)),
  ].filter(Boolean);

  // FIX: projeto vazio → fallback de 3 meses a partir de hoje
  if (!dates.length) {
    const today = new Date();
    const next  = new Date(today);
    next.setMonth(next.getMonth() + 3);
    today.setDate(1);
    return { min: today, max: next };
  }

  const min = new Date(Math.min(...dates));
  const max = new Date(Math.max(...dates));
  min.setDate(1);
  max.setMonth(max.getMonth() + 1, 0);
  return { min, max };
}


function getMonths(min, max) {
  const months = [];
  const current = new Date(min);
  current.setDate(1);
  while (current <= max) {
    months.push(new Date(current));
    current.setMonth(current.getMonth() + 1);
  }
  return months;
}

function daysBetween(a, b) {
  return Math.max(1, Math.round((dateValue(b) - dateValue(a)) / dayMs) + 1);
}

function taskDuration(task) {
  return daysBetween(
    task.start,
    task.end
  );
}

function metric(label, value, hint) {
  return `<article class="metric"><span>${label}</span><strong>${value}</strong><em>${hint}</em></article>`;
}

function renderMetrics() {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const total     = state.tasks.length;
  const completed = state.tasks.filter(t =>
    t.status === "Concluded" || t.status === "Concluded with a delay"
  ).length;
  const late = state.tasks.filter(t =>
    dateValue(t.end) < today &&
    t.status !== "Concluded" &&
    t.status !== "Concluded with a delay"
  ).length;
  const components     = new Set(state.tasks.map(t => t.component)).size;
  const activeProjects = state.projects.filter(p => p.status !== "Archived").length; // FIX
  const pct            = total ? Math.round((completed / total) * 100) : 0;           // FIX

  $("#metricsGrid").innerHTML = [
    metric("Projetos ativos",   activeProjects, "em andamento"),
    metric("Atividades totais", total,          `${components} componentes`),
    metric("Concluídas",        completed,      `${pct}% do cronograma`),
    metric("Alertas abertos",   late,           "atrasadas ou atenção"),
  ].join("");
}

function renderDashboard() {
  renderMetrics();
  renderDashboardProjectsTable();
  renderDashboardMilestonesTable();
}

function renderDashboardProjectsTable() {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const projStatusMeta = {
    "Active":    { color: "#4a76b8", soft: "#dbe8f8", text: "#17365d" },
    "On Hold":   { color: "#b87900", soft: "#fff1c7", text: "#5d3b00" },
    "Completed": { color: "#367c4d", soft: "#dff3ef", text: "#1a4d2e" },
    "Archived":  { color: "#6f7885", soft: "#e7ebf0", text: "#303640" },
  };

  const visible = state.projects.filter(p => p.status !== "Archived");

  const rows = visible.map(proj => {
    const tasks     = proj.tasks || [];
    const concluded = tasks.filter(t =>
      t.status === "Concluded" || t.status === "Concluded with a delay"
    ).length;
    const late = tasks.filter(t =>
      dateValue(t.end) < today &&
      t.status !== "Concluded" &&
      t.status !== "Concluded with a delay"
    ).length;
    const progress = tasks.length ? Math.round((concluded / tasks.length) * 100) : 0;
    const meta     = projStatusMeta[proj.status] || projStatusMeta["Active"];
    const isActive = proj.id === state.activeProjectId;

    // Data final: campo do projeto ou última task
    const endDate = proj.endDate
      ? formatDate(proj.endDate)
      : tasks.length
        ? formatDate(tasks.reduce((m, t) => t.end > m ? t.end : m, ""))
        : "—";

    return `<tr
      class="${isActive ? "row-active-project" : ""}"
      style="cursor:pointer"
      data-switch-project="${proj.id}"
    >
      <td>
        <strong>${escapeHtml(proj.name)}</strong>
        ${isActive ? '<span class="active-project-dot" title="Projeto ativo"></span>' : ""}
        ${proj.client ? `<small style="display:block;color:var(--muted);font-size:11px">${escapeHtml(proj.client)}</small>` : ""}
      </td>
      <td>${proj.manager ? escapeHtml(proj.manager) : '<span style="color:var(--muted)">—</span>'}</td>
      <td>
        <span class="status-badge" style="background:${meta.soft};color:${meta.text}">
          ${escapeHtml(proj.status)}
        </span>
      </td>
      <td>
        <div class="progress-cell">
          <div class="progress-bar-wrap">
            <div class="progress-bar-fill" style="width:${progress}%;background:${meta.color}"></div>
          </div>
          <span class="progress-pct">${progress}%</span>
        </div>
      </td>
      <td>${endDate}</td>
      <td>${late > 0
        ? `<span class="alert-badge">${late}</span>`
        : '<span style="color:var(--muted)">—</span>'
      }</td>
      <td>
        <div class="project-row-actions">
          <button class="row-action" data-edit-project="${proj.id}"      title="Editar"><i data-lucide="pencil"></i></button>
          <button class="row-action" data-duplicate-project="${proj.id}" title="Duplicar"><i data-lucide="copy"></i></button>
          <button class="row-action" data-archive-project="${proj.id}"   title="Arquivar"><i data-lucide="archive"></i></button>
        </div>
      </td>
    </tr>`;
  });

  if (!rows.length) {
    rows.push(`<tr><td colspan="7"><div class="empty-state" style="padding:24px">Nenhum projeto ativo.</div></td></tr>`);
  }

  document.getElementById("dashboardProjectsTable").innerHTML = `
    <thead>
      <tr>
        <th>Projeto</th><th>Gerente</th><th>Status</th>
        <th>Progresso</th><th>Data Final</th><th>Alertas</th><th></th>
      </tr>
    </thead>
    <tbody>${rows.join("")}</tbody>
  `;
}


function renderDashboardMilestonesTable() {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  // Coleta milestones do projeto + task milestones com data futura ou recente (últimos 30 dias)
  const cutoff = new Date(today);
  cutoff.setDate(cutoff.getDate() - 30);

  const items = [];

  // Milestones globais
  state.milestones.forEach(m => {
    if (!m.start) return;
    const d = dateValue(m.start);
    if (d >= cutoff) {
      items.push({
        project: state.projectName || "—",
        name: m.name,
        date: m.start,
        daysLeft: Math.ceil((d - today) / dayMs),
      });
    }
  });

  // Task milestones
  state.tasks.forEach(t => {
    if (!t.milestoneDate) return;
    const d = dateValue(t.milestoneDate);
    if (d >= cutoff) {
      items.push({
        project: escapeHtml(t.component),
        name: t.milestone || "Milestone",
        date: t.milestoneDate,
        daysLeft: Math.ceil((d - today) / dayMs),
      });
    }
  });

  // Ordena por data
  items.sort((a, b) => dateValue(a.date) - dateValue(b.date));

  const rows = items.slice(0, 10).map(item => {
    const isLate   = item.daysLeft < 0;
    const isClose  = item.daysLeft >= 0 && item.daysLeft <= 7;
    const daysLabel = isLate
      ? `<span style="color:var(--red);font-weight:700">${Math.abs(item.daysLeft)}d atrasado</span>`
      : isClose
        ? `<span style="color:var(--amber);font-weight:700">${item.daysLeft}d restantes</span>`
        : `<span style="color:var(--muted)">${item.daysLeft}d restantes</span>`;

    return `<tr>
      <td>${escapeHtml(item.project)}</td>
      <td><strong>${escapeHtml(item.name)}</strong></td>
      <td>${formatDate(item.date)}</td>
      <td>${daysLabel}</td>
    </tr>`;
  });

  $("#dashboardMilestonesTable").innerHTML = `
    <thead>
      <tr>
        <th>Projeto</th>
        <th>Marco</th>
        <th>Data</th>
        <th>Dias restantes</th>
      </tr>
    </thead>
    <tbody>${rows.join("") || `<tr><td colspan="4" class="empty-state" style="padding:24px">Nenhum marco próximo.</td></tr>`}</tbody>
  `;
}


function renderTeamView() {
  const p = getActiveProject();

  // Título com nome do projeto
  const titleEl = document.getElementById("teamProjectTitle");
  if (titleEl) titleEl.textContent = `Equipe — ${p?.name || "Projeto"}`;

  const container = document.getElementById("teamCards");
  if (!container) return;

  const team = p?.team || [];

  if (!team.length) {
    container.innerHTML = `
      <div class="team-empty">
        <i data-lucide="users" style="width:40px;height:40px;color:var(--muted)"></i>
        <p>Nenhum membro na equipe.</p>
        <small>Use o formulário abaixo para adicionar o primeiro membro.</small>
      </div>`;
    return;
  }

  // Ordena: Gerentes primeiro, depois Analistas, ambos em ordem alfabética
  const sorted = [...team].sort((a, b) => {
    if (a.role === b.role) return a.name.localeCompare(b.name);
    return a.role === "Gerente" ? -1 : 1;
  });

  const roleMeta = {
    "Gerente":  { color: "var(--teal)",   soft: "var(--teal-soft)",  icon: "shield-check" },
    "Analista": { color: "var(--indigo)", soft: "var(--indigo-soft)", icon: "bar-chart-2"  },
  };

  container.innerHTML = sorted.map(member => {
    const meta    = roleMeta[member.role] || roleMeta["Analista"];
    const initials = member.name
      .split(" ")
      .map(w => w[0])
      .slice(0, 2)
      .join("")
      .toUpperCase();

    return `
      <div class="team-card">
        <div class="team-card-avatar" style="background:${meta.soft};color:${meta.color}">
          ${initials}
        </div>
        <div class="team-card-info">
          <strong>${escapeHtml(member.name)}</strong>
          <span class="team-role-badge" style="background:${meta.soft};color:${meta.color}">
            <i data-lucide="${meta.icon}"></i>
            ${escapeHtml(member.role)}
          </span>
        </div>
        <button
          class="row-action team-remove-btn"
          data-remove-member="${member.id}"
          title="Remover ${escapeHtml(member.name)}"
        >
          <i data-lucide="x"></i>
        </button>
      </div>`;
  }).join("");
}


function renderFilters() {
  const cf = document.getElementById("componentFilter");
  if (!cf) return;
  const components = ["all", ...new Set(state.tasks.map((task) => task.component))];
  const current = cf.value || "all";
  cf.innerHTML = components
    .map((item) => `<option value="${escapeHtml(item)}">${item === "all" ? "All components" : escapeHtml(item)}</option>`)
    .join("");
  cf.value = components.includes(current) ? current : "all";
}

function renderLegend() {
  const statuses = ["On time", "Attention point", "Delayed", "Concluded", "Concluded with a delay"];
  const html = $("#legendToggle")?.checked
    ? statuses.map((status) => `<span class="legend-item"><span class="dot" style="--dot:${statusMeta[status].color}"></span>${status}</span>`).join("")
    : "";
  const legend = $("#legend");
  if (legend) legend.innerHTML = html;
  const compactLegend = $("#compactLegend");
  if (compactLegend) compactLegend.innerHTML = html;
}

function renderDependencyLines(tasks, rowPositions, min, pxPerDay, timelineWidth) {
  const visibleTaskIds = new Set(tasks.map((task) => task.id));
  const lines = [];

  const headerHeight = 48;
  const milestoneHeight = 52;
  const componentHeight = 38;
  const taskHeight = 44;

  for (const dep of state.dependencies) {

    // ✅ filtro de tasks visíveis
    if (
      !visibleTaskIds.has(dep.predecessorId) ||
      !visibleTaskIds.has(dep.successorId)
    ) {
      continue;
    }

    // ✅ NOVO filtro (evita erro com tasks recolhidas)
    if (
      !rowPositions[dep.predecessorId] ||
      !rowPositions[dep.successorId]
    ) {
      continue;
    }

    const predecessor = state.tasks.find(
      (task) => task.id === dep.predecessorId
    );

    const successor = state.tasks.find(
      (task) => task.id === dep.successorId
    );

    if (!predecessor || !successor) continue;

    const predecessorStart =
      ((dateValue(predecessor.start) - min) / dayMs) * pxPerDay;

    const predecessorEnd =
      ((dateValue(predecessor.end) - min) / dayMs) * pxPerDay;

    const successorStart =
      ((dateValue(successor.start) - min) / dayMs) * pxPerDay;

    const successorEnd =
      ((dateValue(successor.end) - min) / dayMs) * pxPerDay;

    const fromX =
      dep.type === "SS" || dep.type === "SF"
        ? predecessorStart
        : predecessorEnd;

    const toX =
      dep.type === "FF" || dep.type === "SF"
        ? successorEnd
        : successorStart;

    const fromY =
      headerHeight +
      milestoneHeight +
      rowPositions[predecessor.id].offset +
      taskHeight / 2;

    const toY =
      headerHeight +
      milestoneHeight +
      rowPositions[successor.id].offset +
      taskHeight / 2;

    const outGap = 18;
    const verticalX = Math.max(fromX + outGap, toX - outGap);

    lines.push(`
      <path
        class="dependency-line"
        d="
          M ${fromX} ${fromY}
          L ${verticalX} ${fromY}
          L ${verticalX} ${toY}
          L ${toX} ${toY}
        "
      ></path>

      <text
        class="dependency-label"
        x="${verticalX + 4}"
        y="${(fromY + toY) / 2 - 6}"
      >${dep.type}</text>
    `);
  }

  return `
    <svg
      class="dependency-layer"
      width="${timelineWidth}"
      height="${Math.max(300, Object.keys(rowPositions).length * 44 + 220)}"
    >
      <defs>
        <marker
          id="dependencyArrow"
          markerWidth="8"
          markerHeight="8"
          refX="7"
          refY="4"
          orient="auto"
        >
          <path
            d="M 0 0 L 8 4 L 0 8 z"
            class="dependency-arrow"
          ></path>
        </marker>
      </defs>

      ${lines.join("")}
    </svg>
  `;
}

function visualTimelineHeight(rowPositions) {
  return Math.max(300, Object.keys(rowPositions).length * 44 + 120);
}

function isComponentCollapsed(component) {
  return Boolean(collapsedComponents[component]);
}


function initMilestoneDragging() {
  document
    .querySelectorAll(
      ".draggable-milestone"
    )
    .forEach((marker) => {
      marker.addEventListener(
        "mousedown",
        startMilestoneDrag
      );
    });
}

function startMilestoneDrag(event) {
  event.preventDefault();
  event.stopPropagation();

  milestoneDragState = {
    id:
      event.currentTarget.dataset
        .milestoneId,

    taskId:
      event.currentTarget.dataset
        .taskId,

    type:
      event.currentTarget.dataset
        .milestoneType,

    startX: event.clientX,
  };

  commitState();

  document.body.classList.add(
    "dragging"
  );
}

function renderTaskLane(task, min, pxPerDay, width) {
  const meta = statusMeta[task.status || ""] || statusMeta[""];

  const left = Math.max(
    0,
    ((dateValue(task.start) - min) / dayMs) * pxPerDay
  );

  const barWidth = Math.max(
    10,
    daysBetween(task.start, task.end) * pxPerDay
  );

  const progress = Math.max(
    0,
    Math.min(1, Number(task.progress || 0))
  );

  // ✅ LOCKS
  const isStartLocked = isTaskFieldLockedByDependency(task.id, "start");
  const isEndLocked = isTaskFieldLockedByDependency(task.id, "end");

  const activeBaseline = task.baselines?.find(
    (b) => b.id === activeBaselineId
  );
  
  const baselineBar =
    activeBaseline &&
    dateValue(activeBaseline.start) &&
    dateValue(activeBaseline.end)
      ? `
        <div
          class="baseline-bar"
          style="
            left:${
              ((dateValue(activeBaseline.start) - min) / dayMs) * pxPerDay
            }px;
            width:${
              daysBetween(
                activeBaseline.start,
                activeBaseline.end
              ) * pxPerDay
            }px;
          "
        ></div>
      `
      : "";
  


  // ✅ TASK MILESTONE
  const individual = task.milestoneDate
    ? `
    <span
      class="marker draggable-milestone"
      data-task-id="${task.id}"
      data-milestone-type="task"
      title="${escapeHtml(task.milestone || "Milestone")}"
      style="
        left:${((dateValue(task.milestoneDate) - min) / dayMs) * pxPerDay}px;
        --marker:${statusMeta[
          task.milestoneStatus ||
          task.status ||
          ""
        ].color}
      "
    ></span>
    `
    : "";

  // ✅ FREE MILESTONES
  const freeMarkers = state.freeMilestones
    .filter((item) => item.component === task.component)
    .map((item) => {
      const offset =
        ((dateValue(item.date) - min) / dayMs) * pxPerDay;

      if (offset < 0 || offset > width) return "";

      return `<span
        class="marker free draggable-milestone"
        data-milestone-id="${item.id}"
        data-milestone-type="free"
        title="${escapeHtml(item.name)}"
        style="
          left:${offset}px;
          top:${25 + Number(item.level || 0) * 3}px;
          --marker:${(statusMeta[item.status] || statusMeta[""]).color}
        "
      ></span>`;
    })
    .join("");

    return `<div class="lane" style="width:${width}px">
    ${baselineBar}
    <div
      class="bar draggable-bar"
      data-task-id="${task.id}"
      style="
        left:${left}px;
        width:${barWidth}px;
        --bar:${meta.color};
        --bar-border:${darkenHex(meta.color)};
        --bar-soft:${meta.soft};
        --bar-text:${meta.text}
      "
    >

      <!-- ✅ HANDLE ESQUERDO -->
      <span
        class="resize-handle left ${isStartLocked ? "locked" : ""}"
        data-resize="start"
        data-task-id="${task.id}"
      ></span>

      <div
        class="bar-progress"
        style="width:${progress * 100}%"
      ></div>

      <div class="bar-label">
        ${escapeHtml(task.description || "")}
      </div>

      <!-- ✅ HANDLE DIREITO -->
      <span
        class="resize-handle right ${isEndLocked ? "locked" : ""}"
        data-resize="end"
        data-task-id="${task.id}"
      ></span>

    </div>

    ${individual}${freeMarkers}
  </div>`;
}

function getSnapDays(targetZoom = zoom) {
  const snapByZoom = {
    week: 7,
    month: 1,
    quarter: 30,
  };

  return snapByZoom[targetZoom] || 1;
}

function getSnappedDeltaDays(deltaPx, pxPerDay, targetZoom = zoom) {
  const rawDays = Math.round(deltaPx / pxPerDay);
  const snapDays = getSnapDays(targetZoom);

  if (Math.abs(rawDays) < snapDays) {
    return 0;
  }

  return Math.trunc(rawDays / snapDays) * snapDays;
}

function renderMilestones(min, pxPerDay) {
  return state.milestones
    .filter(item => !item.component)
    .map((item, index) => {
      if (!item.start) return "";
      const left = ((dateValue(item.start) - min) / dayMs) * pxPerDay;
      const colors = ["#4a5aa6", "#243782", "#b87900", "#c74444"];
      return `<span class="marker draggable-milestone" data-milestone-id="${item.id}" data-milestone-type="project" style="left:${left}px;top:${20 + Number(item.level || 0) * 16}px;--marker:${colors[item.color] || colors[index % colors.length]}"></span>
      <span class="milestone-label" style="left:${left}px;top:${2 + Number(item.level || 0) * 16}px">${escapeHtml(item.name)}</span>`;
    })
    .join("");
}

function tableInput(value, type, dataset) {
  return `<input type="${type}" value="${escapeAttr(value ?? "")}" ${dataset}>`;
}

function tableSelect(value, options, dataset) {
  return `<select ${dataset}>${options.map((option) => `<option ${option === value ? "selected" : ""}>${escapeHtml(option)}</option>`).join("")}</select>`;
}

function renderTasksTable() {
  const statuses = ["", "On time", "Attention point", "Delayed", "Concluded", "Concluded with a delay"];
  const table = document.getElementById("tasksTable");
  if (!table) return;

  if (!state.tasks.length) {
    table.innerHTML = `<thead><tr><th colspan="10">
      <div class="view-empty-state" style="text-align:center;padding:20px">
        <i data-lucide="inbox"></i>
        <p style="margin:8px 0 4px;font-weight:bold">Nenhuma atividade cadastrada</p>
        <small style="color:#666">Clique em "+ Activity" para começar.</small>
      </div></th></tr></thead><tbody></tbody>`;
    return;
  }

  const si = document.getElementById("searchInput");
  const cf = document.getElementById("componentFilter");
  const sf = document.getElementById("statusFilter");
  const hasFilter = (si?.value || "") !== "" ||
    (cf?.value || "all") !== "all" ||
    (sf?.value || "all") !== "all";

  const filtered = getSortedTasks(getFilteredTasks());

  // Group by component preserving sort order
  const grouped = new Map();
  for (const task of filtered) {
    if (!grouped.has(task.component)) grouped.set(task.component, []);
    grouped.get(task.component).push(task);
  }

  const allSelected = filtered.length > 0 && filtered.every(t => selectedTaskIds.has(t.id));

  const sortIcon = (field) => {
    if (sortState.field !== field) return '<i data-lucide="chevrons-up-down" class="sort-icon"></i>';
    return sortState.dir === 'asc'
      ? '<i data-lucide="chevron-up" class="sort-icon active"></i>'
      : '<i data-lucide="chevron-down" class="sort-icon active"></i>';
  };

  let bodyHtml = '';
  if (filtered.length === 0) {
    bodyHtml = `<tr><td colspan="10"><div class="view-empty-state" style="text-align:center;padding:20px">
      <i data-lucide="search-x"></i><p>Nenhuma atividade encontrada</p>
      <small>Ajuste os filtros para ver resultados.</small></div></td></tr>`;
  } else {
    for (const [component, tasks] of grouped.entries()) {
      // undefined = collapsed (default); false = expanded; filter forces expand
      const isCollapsed = !hasFilter && collapsedComponents[component] !== false;
      const pct = Math.round(tasks.reduce((s, t) => s + (t.progress ?? 0), 0) / tasks.length * 100);
      const delayed = tasks.filter(t => t.status === "Delayed").length;
      const attention = tasks.filter(t => t.status === "Attention point").length;
      const badge = delayed
        ? `<span class="group-badge delayed">${delayed} delayed</span>`
        : attention
          ? `<span class="group-badge attention">${attention} attn</span>`
          : '';

      bodyHtml += `<tr class="group-header-row" data-group="${escapeAttr(component)}">
        <td colspan="10"><div class="group-header-inner">
          <i data-lucide="${isCollapsed ? 'chevron-right' : 'chevron-down'}" class="group-chevron" style="transition:transform .15s"></i>
          <strong>${escapeHtml(component)}</strong>
          <span class="group-meta">${tasks.length} atividade${tasks.length !== 1 ? 's' : ''} · ${pct}%</span>
          ${badge}
        </div></td></tr>`;

      if (!isCollapsed) {
        for (const task of tasks) {
          const chk = selectedTaskIds.has(task.id);
          bodyHtml += `<tr data-id="${task.id}"${chk ? ' class="row-selected"' : ''}>
            <td class="col-check"><input type="checkbox" class="row-check" data-check-id="${task.id}"${chk ? ' checked' : ''} /></td>
            <td>${tableInput(task.component, "text", 'data-field="component"')}</td>
            <td>${tableInput(task.description, "text", 'data-field="description"')}</td>
            <td>${tableInput(task.start, "date", 'data-field="start"')}</td>
            <td>${tableInput(task.end, "date", 'data-field="end"')}</td>
            <td>${tableSelect(task.status || "", statuses, 'data-field="status"')}</td>
            <td>${tableInput(task.progress ?? 0, "number", 'min="0" max="1" step="0.05" data-field="progress"')}</td>
            <td>${tableInput(task.milestone, "text", 'data-field="milestone"')}</td>
            <td>${tableInput(task.milestoneDate, "date", 'data-field="milestoneDate"')}</td>
            <td><button class="row-action" data-delete-task="${task.id}" title="Delete"><i data-lucide="trash-2"></i></button></td>
          </tr>`;
        }
      }
    }
  }

  table.innerHTML = `
    <thead><tr>
      <th class="col-check"><input type="checkbox" id="selectAllCheck"${allSelected ? ' checked' : ''} title="Selecionar tudo" /></th>
      <th class="sortable" data-sort="component">Component ${sortIcon('component')}</th>
      <th class="sortable" data-sort="description">Description ${sortIcon('description')}</th>
      <th class="sortable" data-sort="start">Initial ${sortIcon('start')}</th>
      <th class="sortable" data-sort="end">Final ${sortIcon('end')}</th>
      <th class="sortable" data-sort="status">Status ${sortIcon('status')}</th>
      <th class="sortable" data-sort="progress">% ${sortIcon('progress')}</th>
      <th>Milestone</th>
      <th>Date</th>
      <th></th>
    </tr></thead>
    <tbody>${bodyHtml}</tbody>`;
}

function renderMilestoneTables() {
  const componentOptions = ["", ...new Set(state.tasks.map(t => t.component).filter(Boolean))];

  const rows = state.milestones.length
    ? state.milestones.map(item => `<tr data-id="${item.id}">
        <td>${tableInput(item.name, "text", 'data-ms-field="name"')}</td>
        <td>${tableSelect(item.type, ["Main Milestone", "Milestone", "Phase"], 'data-ms-field="type"')}</td>
        <td>${tableInput(item.start, "date", 'data-ms-field="start"')}</td>
        <td>${tableInput(item.end, "date", 'data-ms-field="end"')}</td>
        <td>${tableSelect(item.component || "", componentOptions, 'data-ms-field="component"')}</td>
        <td>${tableInput(item.description || "", "text", 'placeholder="Optional description" data-ms-field="description"')}</td>
        <td>${tableInput(item.level ?? 0, "number", 'min="0" max="10" data-ms-field="level"')}</td>
        <td><button class="row-action" data-delete-ms="${item.id}" title="Delete"><i data-lucide="trash-2"></i></button></td>
      </tr>`).join("")
    : `<tr><td colspan="8"><div class="view-empty-state" style="padding:24px">
        <i data-lucide="milestone"></i>
        <p>No milestones yet</p>
        <small>Click "+ Milestone" to add one.</small>
      </div></td></tr>`;

  $("#milestonesTable").innerHTML = `
    <thead><tr>
      <th>Name</th><th>Type</th><th>Start</th><th>End</th>
      <th>Component</th><th>Description</th><th>Level</th><th></th>
    </tr></thead>
    <tbody>${rows}</tbody>`;
}

function getAlerts() {
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  return state.tasks
    .filter((task) => task.status !== "Concluded")
    .map((task) => {
      const end = dateValue(task.end);
      const days = Math.ceil((end - today) / dayMs);
      let category = "";
      if (days < 0) category = "Overdue";
      else if (days <= 7) category = "Next 7 days";
      else if (days <= 15) category = "Next 15 days";
      return category ? { task, days, category } : null;
    })
    .filter(Boolean);
}

function getDependencyForTask(taskId) {
  return state.dependencies.find(
    (dep) => dep.successorId === taskId
  );
}

function dependencySelect(task) {
  const dependency = getDependencyForTask(task.id);

  const options = [
    `<option value="">None</option>`,
    ...state.tasks
      .filter((item) => item.id !== task.id)
      .map(
        (item) =>
          `<option value="${item.id}" ${
            dependency?.predecessorId === item.id ? "selected" : ""
          }>
            ${escapeHtml(item.component)} - ${escapeHtml(item.description)}
          </option>`
      ),
  ];

  return `<select data-dependency-predecessor="${task.id}">
    ${options.join("")}
  </select>`;
}


function dependencyTypeSelect(task) {
  const dependency = getDependencyForTask(task.id);

  return `<select data-dependency-type="${task.id}">
    ${["FS", "SS", "FF", "SF"]
      .map(
        (type) =>
          `<option value="${type}" ${
            dependency?.type === type ? "selected" : ""
          }>${type}</option>`
      )
      .join("")}
  </select>`;
}

function renderAlerts() {
  const alerts  = getAlerts();
  const countEl = $("#alertCount");

  if (countEl) {
    countEl.textContent = alerts.length;
    countEl.hidden = alerts.length === 0; // oculta badge quando zerado
  }

  $("#alertsList").innerHTML = alerts.length
    ? alerts.map(alert => {
        const color = alert.days < 0
          ? "var(--red)"
          : alert.days <= 7 ? "var(--amber)" : "var(--teal)";
        const due = alert.days < 0
          ? `${Math.abs(alert.days)} days late`
          : `${alert.days} days left`;
        return `<article class="alert-card" style="--alert:${color}">
          <strong>${escapeHtml(alert.category)} · ${escapeHtml(alert.task.component)}</strong>
          <span>${escapeHtml(alert.task.description)} · ${formatDate(alert.task.end)} · ${due} · ${escapeHtml(alert.task.status || "No status")}</span>
        </article>`;
      }).join("")
    : `<div class="empty-state">Nenhum alerta aberto.</div>`;
}


function getWeekNumber(date) {
  const current = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
  const dayNumber = current.getUTCDay() || 7;

  current.setUTCDate(current.getUTCDate() + 4 - dayNumber);

  const yearStart = new Date(Date.UTC(current.getUTCFullYear(), 0, 1));

  return Math.ceil((((current - yearStart) / dayMs) + 1) / 7);
}

function renderCompactWeeksHeader(min, max, pxPerDay, timelineWidth) {
  const showWeeks = $("#weeksToggle")?.checked;

  if (!showWeeks) return "";

  const weeks = [];
  const current = new Date(min);

  while (current <= max) {
    const weekStart = new Date(current);
    const left = ((weekStart - min) / dayMs) * pxPerDay;

    weeks.push(`
      <div
        class="compact-week-cell"
        style="left:${left}px;width:${7 * pxPerDay}px"
      >
        ${getWeekNumber(weekStart)}
      </div>
    `);

    current.setDate(current.getDate() + 7);
  }

  return `
    <div class="timeline-corner compact-week-corner">Weeks</div>
    <div class="compact-weeks-row" style="width:${timelineWidth}px">
      ${weeks.join("")}
    </div>
  `;
}

function renderCompactMonthBands(months, min, max, pxPerDay) {
  return months
    .map((month, index) => {
      const next = new Date(month);
      next.setMonth(next.getMonth() + 1);

      const left = ((Math.max(month, min) - min) / dayMs) * pxPerDay;
      const width =
        Math.max(
          1,
          Math.round((Math.min(next, max) - Math.max(month, min)) / dayMs)
        ) * pxPerDay;

      return `
        <div
          class="compact-month-band ${index % 2 ? "alt" : ""}"
          style="left:${left}px;width:${width}px"
        ></div>
      `;
    })
    .join("");
}

function renderCompactYearRow(min, max, pxPerDay, timelineWidth) {
  const segs = [];
  for (let y = min.getFullYear(); y <= max.getFullYear(); y++) {
    const yStart = new Date(Math.max(new Date(y, 0, 1), min));
    const yEnd   = new Date(Math.min(new Date(y, 11, 31), max));
    const left   = ((yStart - min) / dayMs) * pxPerDay;
    const width  = (Math.round((yEnd - yStart) / dayMs) + 1) * pxPerDay;
    segs.push(`<div class="compact-year-cell" style="left:${left}px;width:${width}px">${y}</div>`);
  }
  return `
    <div class="timeline-corner compact-year-corner"></div>
    <div class="compact-years-row" style="width:${timelineWidth}px">${segs.join("")}</div>`;
}

const projectMilestoneColors = ["#4a5aa6", "#147c73", "#b87900", "#c74444"];

function renderCompactMilestonesLane(min, pxPerDay, timelineWidth, monthBands, bodyHeight, todayOffsetPx) {
  const items = state.milestones.filter(m => m.start && !m.component);
  const todayLine = (todayOffsetPx >= 0 && todayOffsetPx <= timelineWidth)
    ? `<div class="today-line-ms" style="left:${todayOffsetPx}px;height:${24 + bodyHeight}px">
         <span class="today-label">hoje</span>
       </div>`
    : '';
  if (!items.length && !todayLine) return "";

  const markers = items.map((item, index) => {
    const color = projectMilestoneColors[item.color] || projectMilestoneColors[index % projectMilestoneColors.length];
    const left  = ((dateValue(item.start) - min) / dayMs) * pxPerDay;
    const tip   = `${escapeAttr(item.name)} · ${formatDate(item.start)}${item.end ? " - " + formatDate(item.end) : ""}`;

    if (item.type === "Phase" && item.end) {
      const width = Math.max(28, daysBetween(item.start, item.end) * pxPerDay);
      return `
        <div class="compact-ms-phase compact-ms-draggable"
          data-compact-ms-id="${item.id}"
          title="${tip}"
          style="left:${left}px;width:${width}px;--ms:${color};--ms-border:${darkenHex(color)}"
        >${escapeHtml(item.name)}</div>`;
    }

    if (item.type === "Main Milestone") {
      return `
        <div class="compact-ms-label compact-ms-draggable" data-compact-ms-id="${item.id}" title="${tip}" style="left:${left}px;color:${color}">${escapeHtml(item.name)}</div>
        <div class="compact-ms-mainline" style="left:${left}px;height:${24 + bodyHeight}px;border-color:${color}"></div>
        <div class="compact-ms-triangle compact-ms-draggable" data-compact-ms-id="${item.id}" title="${tip}" style="left:${left}px;border-top-color:${color}"></div>`;
    }

    return `
      <div class="compact-ms-label compact-ms-draggable" data-compact-ms-id="${item.id}" title="${tip}" style="left:${left}px;color:${color}">${escapeHtml(item.name)}</div>
      <div class="compact-ms-triangle compact-ms-draggable" data-compact-ms-id="${item.id}" title="${tip}" style="left:${left}px;border-top-color:${color}"></div>`;
  }).join("");

  return `
    <div class="timeline-corner compact-ms-corner">Milestones</div>
    <div class="compact-milestones-lane" style="width:${timelineWidth}px">
      ${monthBands}
      ${markers}
      ${todayLine}
    </div>`;
}

function renderCompactTimeline() {
  const tasks = getFilteredTasks().sort(
    (a, b) => a.component.localeCompare(b.component) || dateValue(a.start) - dateValue(b.start)
  );
  if (!tasks.length) {
    const ct = document.getElementById("compactTimeline");
    if (ct) ct.innerHTML = `
      <div class="view-empty-state" style="padding:80px 24px">
        <i data-lucide="layout-template"></i>
        <p>Nenhuma atividade para exibir</p>
        <small>${
          state.tasks.length
            ? "Ajuste os filtros para ver atividades."
            : "Adicione atividades na aba Activities."
        }</small>
      </div>`;
    lucide.createIcons();
    return;
  }
  const { min, max } = getRange(tasks.length ? tasks : state.tasks);
  const totalDays = Math.max(1, Math.round((max - min) / dayMs) + 1);
  const pxPerDay = zoomConfig[compactZoom].pxPerDay * compactMonthScale;
  const timelineWidth = Math.max(680, Math.round(totalDays * pxPerDay));
  const todayRaw = new Date(); todayRaw.setHours(0, 0, 0, 0);
  const todayOffsetPx = Math.round((todayRaw.getTime() - min.getTime()) / dayMs * pxPerDay);
  const months = getMonths(min, max);

  const weeksHeader = renderCompactWeeksHeader(min, max, pxPerDay, timelineWidth);
  const monthBands = renderCompactMonthBands(months, min, max, pxPerDay);

  const monthColumns = months.map((month, index) => {
    const next = new Date(month);
    next.setMonth(next.getMonth() + 1);
    const days = Math.max(1, Math.round((Math.min(next, max) - Math.max(month, min)) / dayMs));
    return `<div class="month-cell" style="width:${days * pxPerDay}px;grid-column:${index + 1}">
      ${compactZoom === "quarter" ? quarterLabel(month) : monthLabel(month)}
    </div>`;
  }).join("");

  const grouped = new Map();
  tasks.forEach((task) => {
    if (!grouped.has(task.component)) grouped.set(task.component, []);
    grouped.get(task.component).push(task);
  });

  const rows = [];
  let bodyHeight = 0;
  for (const [component, componentTasks] of grouped.entries()) {
    const lanes = packCompactTasks(componentTasks);
    const rowHeight = lanes.length * 32 + 20;
    bodyHeight += rowHeight + 1;
    const labelFontSize = Math.round(Math.max(9, Math.min(15, compactLabelColWidth / 20)));

    const compactComponentMilestones = state.milestones
      .filter(m => m.component === component && m.start)
      .map((m, idx) => {
        const left = ((dateValue(m.start) - min) / dayMs) * pxPerDay;
        const colors = ["#4a5aa6", "#243782", "#b87900", "#c74444"];
        const color = colors[m.color] || colors[idx % colors.length];
        const tip = `${escapeAttr(m.name)} · ${formatDate(m.start)}`;
        return `<div class="compact-ms-triangle compact-ms-draggable" data-compact-ms-id="${m.id}" title="${tip}" style="left:${left}px;top:4px;border-top-color:${color}"></div>`;
      }).join("");

    rows.push(`
      <div class="compact-component-label" style="height:${rowHeight}px;width:${compactLabelColWidth}px;min-width:${compactLabelColWidth}px;font-size:${labelFontSize}px">
        <strong>${escapeHtml(component)}</strong>
      </div>
      <div class="compact-component-lane" style="width:${timelineWidth}px;height:${rowHeight}px">
        ${monthBands}
        ${compactComponentMilestones}
        ${lanes.flatMap((lane, laneIndex) => lane.map((task) => renderCompactBar(task, min, pxPerDay, laneIndex))).join("")}
      </div>
    `);
  }

  const yearRow        = renderCompactYearRow(min, max, pxPerDay, timelineWidth);
  const milestonesLane = renderCompactMilestonesLane(min, pxPerDay, timelineWidth, monthBands, bodyHeight, todayOffsetPx);

  const compactTitle = document.getElementById("compactProjectTitle");
  const compactPeriod = document.getElementById("compactPeriodLabel");
  if (compactTitle) compactTitle.textContent = state.projectName || "Timeline Compact";
  if (compactPeriod) {
    const fmt = d => new Intl.DateTimeFormat("pt-BR", { month: "short", year: "numeric" }).format(d);
    compactPeriod.textContent = `${fmt(min)} — ${fmt(max)}`;
  }
  $("#compactTimeline").innerHTML = `
    <div class="timeline-grid compact-grid" style="grid-template-columns:${compactLabelColWidth}px 1fr">
      ${yearRow}
      <div class="timeline-corner" style="min-width:${compactLabelColWidth}px">${tasks.length} activities</div>
      <div class="months" style="grid-template-columns:repeat(${months.length}, auto);width:${timelineWidth}px">
        ${monthColumns}
      </div>
      ${weeksHeader}
      ${milestonesLane}
      ${rows.join("")}
    </div>
  `;

  $$(".compact-component-lane").forEach((lane) => {
    lane.style.backgroundSize = `${compactZoom === "week" ? 63 : compactZoom === "quarter" ? 252 : 126}px 100%`;
  });
_compactExportWidth  = compactLabelColWidth + timelineWidth + 48;
_compactExportHeight = (document.getElementById("compactTimeline")?.scrollHeight || 800) + 80;

  initCompactDragging();
}

function packCompactTasks(tasks) {
  const lanes = [];

  const ordered = [...tasks].sort(
    (a, b) => dateValue(a.start) - dateValue(b.start)
  );

  for (const task of ordered) {
    let placed = false;

    for (const lane of lanes) {
      const last = lane[lane.length - 1];

      if (dateValue(task.start) >= dateValue(last.end)) {
        lane.push(task);
        placed = true;
        break;
      }
    }

    if (!placed) {
      lanes.push([task]);
    }
  }

  return lanes;
}

function compactTaskLabel(task) {
  const durationDays = daysBetween(task.start, task.end);
  const durationWeeks = Math.max(1, Math.ceil(durationDays / 7));

  if (compactLabelMode === "days") {
    return `${task.description || ""} · ${durationDays}d`;
  }

  if (compactLabelMode === "dates") {
    return `${task.description || ""} · ${formatDate(task.start)} - ${formatDate(task.end)}`;
  }

  return `${task.description || ""} · ${durationWeeks}w`;
}

function renderCompactBar(task, min, pxPerDay, laneIndex) {
  const meta = statusMeta[task.status || ""] || statusMeta[""];

  const left =
    ((dateValue(task.start) - min) / dayMs) * pxPerDay;

  const width =
    Math.max(28, daysBetween(task.start, task.end) * pxPerDay);

  const isNarrow = width < 56;

  return `
    <div
      class="compact-bar compact-draggable-bar${isNarrow ? " compact-bar--narrow" : ""}"
      data-task-id="${task.id}"
      title="${escapeAttr(task.component)} | ${escapeAttr(task.description)}
Status: ${escapeAttr(task.status || "No status")}
Period: ${formatDate(task.start)} → ${formatDate(task.end)}
Duration: ${daysBetween(task.start, task.end)}d"
      style="
        left:${left}px;
        top:${10 + laneIndex * 32}px;
        width:${width}px;
        --bar:${meta.color};
        --bar-border:${darkenHex(meta.color)};
        --bar-soft:${meta.soft};
        --bar-text:${meta.text};
      "
    >
      ${isNarrow ? "" : escapeHtml(compactTaskLabel(task))}
    </div>
  `;
}

function renderAll() {
  // Elementos sempre visíveis (topbar + sidebar)
  const pnEl = document.getElementById("projectName");
  if (pnEl) pnEl.value = state.projectName;
  $("#pageTitle").textContent = state.projectName || "Schedule Generator";
  renderFilters();
  renderLegend();
  renderBaselineSelector();
  renderProjectSelector();
  renderAlerts();

  // Só renderiza a view que está na tela
  const viewId = document.querySelector(".view.active")?.id || "dashboardView";
  if (viewId === "dashboardView")   renderDashboard();
  if (viewId === "compactView")     renderCompactTimeline();
  if (viewId === "activitiesView")  { renderTasksTable(); updateBulkBar(); }
  if (viewId === "milestonesView")  renderMilestoneTables();
  if (viewId === "settingsView")    renderSettingsView();
  if (viewId !== "activitiesView")  { const bb = document.getElementById("bulkBar"); if (bb) bb.style.display = 'none'; }

  // Sidebar-toggles (Weeks/Legend/Baseline) só faz sentido na Timeline
  const sidebarToggles = document.querySelector(".sidebar-toggles");
  if (sidebarToggles) sidebarToggles.style.display = viewId === "compactView" ? "" : "none";

  lucide.createIcons();
}
function initTaskResize() {
  document
    .querySelectorAll(".resize-handle")
    .forEach((handle) => {
      handle.addEventListener(
        "mousedown",
        startResize
      );
    });
}
function startResize(event) {
  event.stopPropagation();
  event.preventDefault();
  const taskId = event.currentTarget.dataset.taskId;

  const edge = event.currentTarget.dataset.resize;
  const field = edge === "start" ? "start" : "end";
  
  if (isTaskFieldLockedByDependency(taskId, field)) {
    const dependency = getTaskPredecessorDependency(taskId);
  
    alert(
      `This side is controlled by the ${dependency.type} dependency.`
    );
  
    return;
  }

  resizeState = {
    taskId,
    edge,
    startX: event.clientX,
  };

  commitState();

  document.body.classList.add("dragging");
}
function updateField(collection, id, field, value) {
  if (collection === "tasks" && isTaskFieldLockedByDependency(id, field)) {
    const dependency = getTaskPredecessorDependency(id);
    const fieldName = field === "start" ? "Start" : "End";
    showToast(`${fieldName} locked by ${dependency.type} dependency`, "warning");
    renderAll();
    return;
  }
  const item = state[collection].find((entry) => entry.id === id);
  if (!item) return;
  commitStateDebounced();
  item[field] = ["progress", "level", "color"].includes(field) ? Number(value || 0) : value;
  saveState();
  scheduleRender();
}


function showToast(message, type = "") {
  const iconMap = {
    success: "check-circle",
    error:   "x-circle",
    warning: "alert-triangle",
    info:    "info",
    "":      "check-circle",
  };

  const toast = document.createElement("div");
  toast.className = `toast${type ? ` toast-${type}` : ""}`;
  toast.innerHTML = `
    <i data-lucide="${iconMap[type] ?? "check-circle"}"></i>
    <span>${escapeHtml(message)}</span>`;

  document.body.appendChild(toast);
  lucide.createIcons({ nodes: [toast] });

  setTimeout(() => toast.classList.add("show"), 10);
  setTimeout(() => {
    toast.classList.remove("show");
    setTimeout(() => toast.remove(), 300);
  }, 2500);
}

function recalculateDependencies(
  sourceTaskId
) {
  const links =
    state.dependencies.filter(
      (dep) =>
        dep.predecessorId ===
        sourceTaskId
    );

  for (const dep of links) {

    const source =
      state.tasks.find(
        (t) => t.id === dep.predecessorId
      );

    const target =
      state.tasks.find(
        (t) => t.id === dep.successorId
      );

    if (!source || !target) continue;

    const duration =
      taskDuration(target);

    const sourceStart =
      dateValue(source.start);

    const sourceEnd =
      dateValue(source.end);

    let newStart;
    let newEnd;

    switch (dep.type) {

      case "FS":
        newStart = new Date(sourceEnd);
        newStart.setDate(newStart.getDate() + 1);

        newEnd = new Date(newStart);
        newEnd.setDate(
          newEnd.getDate() + duration - 1
        );
        break;

      case "SS":
        newStart = new Date(sourceStart);

        newEnd = new Date(newStart);
        newEnd.setDate(
          newEnd.getDate() + duration - 1
        );
        break;

      case "FF":
        newEnd = new Date(sourceEnd);

        newStart = new Date(newEnd);
        newStart.setDate(
          newStart.getDate() - duration + 1
        );
        break;

      case "SF":
        newEnd = new Date(sourceStart);

        newStart = new Date(newEnd);
        newStart.setDate(
          newStart.getDate() - duration + 1
        );
        break;
    }

    target.start = iso(newStart);
    target.end = iso(newEnd);

    recalculateDependencies(target.id);
  }
}


function addDependency(
  predecessorId,
  successorId,
  type = "FS"
) {
  commitState();

  state.dependencies.push({
    id: crypto.randomUUID(),
    predecessorId,
    successorId,
    type
  });

  saveState();
  renderAll();
}


function initTaskDragging() {
  document.querySelectorAll(".draggable-bar").forEach((bar) => {
    bar.addEventListener("mousedown", startTaskDrag);
  });
}

function hasPredecessor(taskId) {
  return state.dependencies.some(
    (dep) => dep.successorId === taskId
  );
}

function getTaskPredecessorDependency(taskId) {
  return state.dependencies.find(
    (dep) => dep.successorId === taskId
  );
}

function getTaskPredecessorDependencies(taskId) {
  return state.dependencies.filter(
    (dep) => dep.successorId === taskId
  );
}

function initCompactDragging() {
  document.querySelectorAll(".compact-draggable-bar").forEach((bar) => {
    bar.onmousedown = startCompactDrag;
  });
  document.querySelectorAll(".compact-ms-draggable").forEach((el) => {
    el.onmousedown = startCompactMsDrag;
  });
}

function startCompactMsDrag(event) {
  event.preventDefault();
  compactMsDragState = {
    id: event.currentTarget.dataset.compactMsId,
    startX: event.clientX,
  };
  commitState();
  document.body.classList.add("dragging");
}

function startCompactDrag(event) {
  event.preventDefault();

  const taskId = event.currentTarget.dataset.taskId;

  if (hasPredecessor(taskId)) {
    showToast("This activity has a predecessor. Move the predecessor instead.");
    return;
  }

  compactDragState = {
    taskId,
    startX: event.clientX,
  };

  commitState();

  document.body.classList.add("dragging");
}

function startTaskDrag(event) {
  event.preventDefault();

  const taskId = event.currentTarget.dataset.taskId;

  if (hasPredecessor(taskId)) {
    alert("This activity has a predecessor. Resize the free side instead.");
    return;
  }

  dragState = {
    taskId,
    startX: event.clientX,
  };

  commitState();

  document.body.classList.add("dragging");
}

function shiftTask(task, days) {
  const start = dateValue(task.start);
  const end = dateValue(task.end);

  start.setDate(start.getDate() + days);
  end.setDate(end.getDate() + days);

  task.start = iso(start);
  recalculateDependencies(task.id);
  task.end = iso(end);
  recalculateDependencies(task.id);

  if (task.milestoneDate) {
    const milestone = dateValue(task.milestoneDate);

    milestone.setDate(
      milestone.getDate() + days
    );

    task.milestoneDate = iso(milestone);
  }
  recalculateDependencies(task.id);
}

document.addEventListener("mousemove", (event) => {
  if (compactMsDragState) {
    const pxPerDay = zoomConfig[compactZoom].pxPerDay * compactMonthScale;
    const deltaPx = event.clientX - compactMsDragState.startX;
    const deltaDays = getSnappedDeltaDays(deltaPx, pxPerDay, compactZoom);
    if (!deltaDays) return;

    const item = state.milestones.find((m) => m.id === compactMsDragState.id);
    if (!item || !item.start) return;

    const start = dateValue(item.start);
    start.setDate(start.getDate() + deltaDays);
    item.start = iso(start);

    if (item.end) {
      const end = dateValue(item.end);
      end.setDate(end.getDate() + deltaDays);
      item.end = iso(end);
    }

    compactMsDragState.startX += deltaDays * pxPerDay;
    saveState();
    renderCompactTimeline();
    return;
  }

  if (compactDragState) {
    const pxPerDay = zoomConfig[compactZoom].pxPerDay * compactMonthScale;
  
    const deltaPx =
      event.clientX - compactDragState.startX;
  
    const deltaDays =
      getSnappedDeltaDays(deltaPx, pxPerDay, compactZoom);
  
    if (!deltaDays) return;
  
    const task = state.tasks.find(
      (item) => item.id === compactDragState.taskId
    );
  
    if (!task) return;
  
    shiftTask(task, deltaDays);
  
    compactDragState.startX += deltaDays * pxPerDay;
  
    saveState();
  
    renderCompactTimeline();
  
    return;
  }

  if (milestoneDragState) {

    const pxPerDay =
    zoomConfig[zoom].pxPerDay;
  
    const deltaPx =
      event.clientX -
      milestoneDragState.startX;
  
      const deltaDays =
      getSnappedDeltaDays(deltaPx, pxPerDay);
  
    if (!deltaDays) return;
  
    if (
      milestoneDragState.type ===
      "project"
    ) {
  
      const item =
        state.milestones.find(
          (m) =>
            m.id ===
            milestoneDragState.id
        );
  
      if (item?.start) {
        const date =
          dateValue(item.start);
  
        date.setDate(
          date.getDate() +
          deltaDays
        );
  
        item.start = iso(date);
      }
    }
  
    if (
      milestoneDragState.type ===
      "free"
    ) {
  
      const item =
        state.freeMilestones.find(
          (m) =>
            m.id ===
            milestoneDragState.id
        );
  
      if (item?.date) {
        const date =
          dateValue(item.date);
  
        date.setDate(
          date.getDate() +
          deltaDays
        );
  
        item.date = iso(date);
      }
    }
  
    if (
      milestoneDragState.type ===
      "task"
    ) {
  
      const task =
        state.tasks.find(
          (t) =>
            t.id ===
            milestoneDragState.taskId
        );
  
      if (
        task &&
        task.milestoneDate
      ) {
  
        const date =
          dateValue(
            task.milestoneDate
          );
  
        date.setDate(
          date.getDate() +
          deltaDays
        );
  
        task.milestoneDate =
          iso(date);
      }
    }
  
    milestoneDragState.startX += deltaDays * pxPerDay;
  
    saveState();
  
    refreshTimelineInteractions();
  
    initTaskDragging();
    initTaskResize();
    initMilestoneDragging();
  }
  if (!dragState) return;

  const pxPerDay =
  zoomConfig[zoom].pxPerDay;

  const deltaPx =
    event.clientX - dragState.startX;

    const deltaDays =
    getSnappedDeltaDays(deltaPx, pxPerDay);

  if (!deltaDays) return;

  const task = state.tasks.find(
    (item) =>
      item.id === dragState.taskId
  );

  if (!task) return;

  shiftTask(task, deltaDays);

  dragState.startX += deltaDays * pxPerDay;

  saveState();

  refreshTimelineInteractions();

  initTaskDragging();
});

document.addEventListener("mouseup", () => {
  const wasInteracting =
  dragState ||
  resizeState ||
  milestoneDragState ||
  compactDragState ||
  compactMsDragState;

  dragState = null;
  resizeState = null;
  milestoneDragState = null;
  compactDragState = null;
  compactMsDragState = null;

  document.body.classList.remove("dragging");

  if (wasInteracting) {
    renderAll();
  }
});


document.addEventListener(
  "mousemove",
  (event) => {
    if (!resizeState) return;

    const pxPerDay =
    zoomConfig[zoom].pxPerDay;

    const deltaPx =
      event.clientX - resizeState.startX;

      const deltaDays =
      getSnappedDeltaDays(deltaPx, pxPerDay);

    if (!deltaDays) return;

    const task = state.tasks.find(
      (item) =>
        item.id === resizeState.taskId
    );

    if (!task) return;

    const start = dateValue(task.start);
    const end = dateValue(task.end);

    if (resizeState.edge === "start") {
      start.setDate(
        start.getDate() + deltaDays
      );

      if (start >= end) {
        start.setDate(end.getDate() - 1);
      }

      task.start = iso(start);
      recalculateDependencies(task.id);
    }

    if (resizeState.edge === "end") {
      end.setDate(
        end.getDate() + deltaDays
      );

      if (end <= start) {
        end.setDate(start.getDate() + 1);
      }

      task.end = iso(end);
      recalculateDependencies(task.id);
    }

    resizeState.startX += deltaDays * pxPerDay;

    saveState();
    refreshTimelineInteractions();

    initTaskDragging();
    initTaskResize();
  }
);
function updateDependencyPredecessor(successorId, predecessorId) {
  commitState();

  state.dependencies = state.dependencies.filter(
    (dep) => dep.successorId !== successorId
  );

  if (predecessorId) {
    state.dependencies.push({
      id: crypto.randomUUID(),
      predecessorId,
      successorId,
      type: "FS",
    });

    recalculateDependencies(predecessorId);
  }

  saveState();
  renderAll();
}
function updateDependencyType(successorId, type) {
  commitState();

  const dependency = getDependencyForTask(successorId);

  if (!dependency) return;

  dependency.type = type;

  recalculateDependencies(dependency.predecessorId);

  saveState();
  renderAll();
}

function validateRequired(inputId) {
  const el = document.getElementById(inputId);
  if (!el) return true;
  const valid = el.value.trim() !== "";
  el.classList.toggle("input-error", !valid);
  if (!valid) {
    el.focus();
    showToast("Campo obrigatório não preenchido", "error");
  }
  return valid;
}

function bindEvents() {

  bindPngDialogEvents();

  document.addEventListener("click", (event) => {
    const componentHeader = event.target.closest(".collapsible-component");
  
    if (!componentHeader) return;
  
    const component = componentHeader.dataset.component;
  
    if (!component) return;
  
    collapsedComponents[component] = !collapsedComponents[component];
  
    refreshTimelineInteractions();
    initTaskDragging();
    initTaskResize();
    initMilestoneDragging();
  });

  const baselineSelect = document.getElementById("baselineSelector");

if (baselineSelect) {
  baselineSelect.addEventListener("change", (event) => {
    activeBaselineId = event.target.value;
    renderAll();
  });
}

$("#weeksToggle").addEventListener("change", () => {
  renderCompactTimeline();
});


$$(".compact-label").forEach((button) => {
  button.addEventListener("click", () => {
    compactLabelMode = button.dataset.compactLabel;

    $$(".compact-label").forEach((item) =>
      item.classList.toggle(
        "active",
        item.dataset.compactLabel === compactLabelMode
      )
    );

    renderCompactTimeline();
  });
});

$$(".compact-zoom").forEach((button) => {
  button.addEventListener("click", () => {
    compactZoom = button.dataset.compactZoom;

    $$(".compact-zoom").forEach((item) =>
      item.classList.toggle(
        "active",
        item.dataset.compactZoom === compactZoom
      )
    );

    renderCompactTimeline();
  });
});

  $("#captureBaselineButton")
  .addEventListener(
    "click",
    captureBaseline
  );

  document.getElementById("dependenciesToggle")?.addEventListener("change", (event) => {
    showDependencies = event.target.checked;
  });

  document.addEventListener("mousedown", (event) => {
    if (
      event.target.matches("select") ||
      event.target.matches("option")
    ) {
      event.stopPropagation();
    }
  });


  document.addEventListener("keydown", (event) => {
    const isMac = navigator.platform.includes("Mac");
  
    const ctrl = isMac ? event.metaKey : event.ctrlKey;
  
    if (!ctrl) return;
  
    if (event.key.toLowerCase() === "z" && !event.shiftKey) {
      event.preventDefault();
      undo();
    }
  
    if (
      event.key.toLowerCase() === "y" ||
      (event.key.toLowerCase() === "z" && event.shiftKey)
    ) {
      event.preventDefault();
      redo();
    }
  });

$$(".nav-tab").forEach((button) => {
  button.addEventListener("click", () => {
    // Flush any pending debounced render so the new view is always up to date.
    if (_renderDebounceTimer) {
      clearTimeout(_renderDebounceTimer);
      _renderDebounceTimer = null;
    }
    $$(".nav-tab").forEach((tab) => tab.classList.remove("active"));
    $$(".view").forEach((view) => view.classList.remove("active"));
    button.classList.add("active");

    const viewId = `#${button.dataset.view}View`;
    const target = $(viewId);
    if (target) target.classList.add("active");
    renderAll();
  });
});

  $("#projectName").addEventListener("input", (event) => {
    state.projectName = event.target.value;
    saveState();
    $("#pageTitle").textContent = state.projectName;
  });

  ["searchInput", "componentFilter", "statusFilter", "legendToggle"].forEach((id) => {
    document.getElementById(id)?.addEventListener("input", renderAll);
  });

  $$(".segmented[data-zoom]").forEach((button) => {
    button.addEventListener("click", () => {
      zoom = button.dataset.zoom;
  
      $$(".segmented[data-zoom]").forEach((item) =>
        item.classList.toggle(
          "active",
          item.dataset.zoom === zoom
        )
      );
  
      refreshTimelineInteractions();
    });
  });
  

  document.addEventListener("change", (event) => {
    // Row checkbox
    if (event.target.classList.contains("row-check")) {
      const id = event.target.dataset.checkId;
      if (event.target.checked) selectedTaskIds.add(id);
      else selectedTaskIds.delete(id);
      const row = event.target.closest("tr[data-id]");
      if (row) row.classList.toggle("row-selected", event.target.checked);
      updateBulkBar();
      return;
    }

    // Select-all checkbox
    if (event.target.id === "selectAllCheck") {
      const vis = getFilteredTasks();
      if (event.target.checked) vis.forEach(t => selectedTaskIds.add(t.id));
      else selectedTaskIds.clear();
      updateBulkBar();
      renderTasksTable();
      lucide.createIcons();
      return;
    }

    if (event.target.dataset.dependencyPredecessor) {
      updateDependencyPredecessor(
        event.target.dataset.dependencyPredecessor,
        event.target.value
      );
      return;
    }
  
    if (event.target.dataset.dependencyType) {
      updateDependencyType(
        event.target.dataset.dependencyType,
        event.target.value
      );
      return;
    }
  
    const row = event.target.closest("tr[data-id]");
    if (!row) return;
  
    if (event.target.dataset.field) {
      updateField("tasks", row.dataset.id, event.target.dataset.field, event.target.value);
    }
  
    if (event.target.dataset.msField) {
      updateField("milestones", row.dataset.id, event.target.dataset.msField, event.target.value);
    }
  
    if (event.target.dataset.freeField) {
      updateField("freeMilestones", row.dataset.id, event.target.dataset.freeField, event.target.value);
    }
  });

  document.addEventListener("click", (event) => {
    // Group header toggle (undefined = collapsed by default; false = explicitly expanded)
    const groupRow = event.target.closest(".group-header-row");
    if (groupRow) {
      const comp = groupRow.dataset.group;
      if (collapsedComponents[comp] === false) {
        delete collapsedComponents[comp]; // collapse (back to default)
      } else {
        collapsedComponents[comp] = false; // expand explicitly
      }
      renderTasksTable();
      lucide.createIcons();
      return;
    }

    // Sortable column header
    const sortTh = event.target.closest(".sortable");
    if (sortTh) {
      const field = sortTh.dataset.sort;
      if (sortState.field === field) {
        sortState.dir = sortState.dir === 'asc' ? 'desc' : 'asc';
      } else {
        sortState.field = field;
        sortState.dir = 'asc';
      }
      renderTasksTable();
      lucide.createIcons();
      return;
    }

    const deleteTask = event.target.closest("[data-delete-task]");
    const deleteMs = event.target.closest("[data-delete-ms]");
    const deleteFree = event.target.closest("[data-delete-free]");
    commitState();
    if (deleteTask) state.tasks = state.tasks.filter((task) => task.id !== deleteTask.dataset.deleteTask);
    if (deleteMs) state.milestones = state.milestones.filter((item) => item.id !== deleteMs.dataset.deleteMs);
    if (deleteFree) state.freeMilestones = state.freeMilestones.filter((item) => item.id !== deleteFree.dataset.deleteFree);
    if (deleteTask || deleteMs || deleteFree) {
      saveState();
      renderAll();
    }
  });

  $("#addTaskButton").addEventListener("click", () => {
    commitState();
    state.tasks.push({
      id: crypto.randomUUID(),
      component: "New component",
      description: "New activity",
      start: iso(new Date()),
      end: iso(new Date(Date.now() + 14 * dayMs)),
      status: "On time",
      progress: 0,
      milestone: "",
      milestoneDate: "",
      milestoneStatus: "",
      level: 0,
    });
    saveState();
    renderAll();
  });

  $("#addMilestoneButton").addEventListener("click", () => {
    commitState();
    state.milestones.push({ id: crypto.randomUUID(), name: "New", type: "Milestone", start: iso(new Date()), end: "", level: 0, color: 1 });
    saveState();
    renderAll();
  });

  $("#addFreeMilestoneButton")?.addEventListener("click", () => {
    commitState();
    state.freeMilestones.push({
      id: crypto.randomUUID(),
      name: "New marker",
      status: "Attention point",
      date: iso(new Date()),
      component: state.tasks[0]?.component || "",
      level: 0,
    });
    saveState();
    renderAll();
  });

  document.getElementById("recalcStatusButton")?.addEventListener("click", recalculateStatuses);
  document.getElementById("bulkApply")?.addEventListener("click", applyBulkEdit);
  document.getElementById("bulkDelete")?.addEventListener("click", bulkDeleteTasks);
  document.getElementById("bulkClear")?.addEventListener("click", () => {
    selectedTaskIds.clear();
    updateBulkBar();
    renderTasksTable();
    lucide.createIcons();
  });

  $("#importJsonButton").addEventListener("click", () => $("#importJsonInput").click());
  $("#importExcelButton").addEventListener("click", () => $("#importExcelInput").click());
  $("#exportExcelButton")?.addEventListener("click", exportExcelProject);
  $("#importExcelInput").addEventListener("change", async (event) => {
    const file = event.target.files?.[0];
    if (!file) return;
    await importExcelProject(file);
    event.target.value = "";
  });

 $("#importJsonInput").addEventListener("change", async (event) => {
    const file = event.target.files?.[0];
    if (!file) return;
    try {
      const imported = JSON.parse(await file.text());
      const isOldFormat = Array.isArray(imported.tasks) && Array.isArray(imported.milestones);
      const isNewFormat = Array.isArray(imported.projects);
      if (!isOldFormat && !isNewFormat) throw new Error("Formato inválido");
      state = normalizeState(imported);
      saveState();
      renderAll();
      showToast("Projeto importado com sucesso");
    } catch {
      alert("Arquivo JSON inválido para o Schedule Studio.");
    } finally {
      event.target.value = "";
    }
  });

  $("#exportJsonButton").addEventListener(
    "click",
    () =>
      download(
        "schedule-studio.json",
        JSON.stringify(
          {
            activeProjectId: state.activeProjectId,
            users:           state.users        ?? [],
            projectUsers:    state.projectUsers ?? [],
            projects:        state.projects
          },
          null, 2
        ),
        "application/json"
      )
  );

  
  $("#exportCsvButton").addEventListener(
    "click",
    () =>
      download(
        "schedule-activities.csv",
        toCsv(state.tasks),
        "text/csv"
      )
  );
  
  // --- Drawer: Layout Settings ---
  const layoutDrawer  = document.getElementById("layoutDrawer");
  const drawerOverlay = document.getElementById("layoutDrawerOverlay");

  function openLayoutDrawer() {
    layoutDrawer.classList.add("open");
    drawerOverlay.classList.add("open");
    document.body.style.overflow = "hidden";
  }

  function closeLayoutDrawer() {
    layoutDrawer.classList.remove("open");
    drawerOverlay.classList.remove("open");
    document.body.style.overflow = "";
  }

  document.getElementById("layoutSettingsButton")
    ?.addEventListener("click", openLayoutDrawer);

  document.getElementById("closeLayoutDrawer")
    ?.addEventListener("click", closeLayoutDrawer);

  drawerOverlay?.addEventListener("click", closeLayoutDrawer);

  // Sincroniza weeksToggleDrawer → weeksToggle (sidebar)
  document.getElementById("weeksToggleDrawer")
    ?.addEventListener("change", (e) => {
      const main = document.getElementById("weeksToggle");
      if (main) {
        main.checked = e.target.checked;
        main.dispatchEvent(new Event("change"));
      }
    });

  // Fecha drawer com Escape
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && layoutDrawer?.classList.contains("open")) {
      closeLayoutDrawer();
    }
  });
  
  // Seletor de projeto (topbar)
  document.getElementById("projectSelector")
    ?.addEventListener("change", e => switchProject(e.target.value));

  // Botão novo projeto
  document.getElementById("newProjectButton")
    ?.addEventListener("click", () => openProjectModal());

  // Modal: fechar
  document.getElementById("closeProjectModal")
    ?.addEventListener("click", () => document.getElementById("projectModal").close());
  document.getElementById("cancelProjectModal")
    ?.addEventListener("click", () => document.getElementById("projectModal").close());

  // Modal: salvar
  document.getElementById("saveProjectModal")
    ?.addEventListener("click", () => {
      const id   = document.getElementById("projectModalId").value;
      const name = document.getElementById("projectModalName").value.trim();
      if (!validateRequired("projectModalName")) return;

      const data = {
        name,
        client:    document.getElementById("projectModalClient").value.trim(),
        manager:   document.getElementById("projectModalManager").value.trim(),
        startDate: document.getElementById("projectModalStart").value,
        endDate:   document.getElementById("projectModalEnd").value,
        status:    document.getElementById("projectModalStatus").value,
      };

      if (id) { updateProject(id, data); }
      else    { createProject(data);     }

      document.getElementById("projectModal").close();
    });

  // Delegação: ações na tabela do dashboard
  document.getElementById("dashboardProjectsTable")
    ?.addEventListener("click", e => {
      const switchBtn    = e.target.closest("[data-switch-project]");
      const editBtn      = e.target.closest("[data-edit-project]");
      const duplicateBtn = e.target.closest("[data-duplicate-project]");
      const archiveBtn   = e.target.closest("[data-archive-project]");

      if (archiveBtn) { archiveProject(archiveBtn.dataset.archiveProject); return; }
      if (duplicateBtn) { duplicateProject(duplicateBtn.dataset.duplicateProject); return; }
      if (editBtn) { openProjectModal(editBtn.dataset.editProject); return; }
      if (switchBtn) { switchProject(switchBtn.dataset.switchProject); }
    });

  // Reset: recria seed com estrutura nova
  // (o #resetButton já existe — SUBSTITUIR seu listener)
  // REMOVER o listener original de #resetButton e usar este:
  document.getElementById("resetButton")
    ?.addEventListener("click", () => {
      if (!confirm("Resetar para os dados de exemplo?")) return;
      const id = crypto.randomUUID();
      state = {
        activeProjectId: id,
        projects: [{
          id, name: seed.projectName, client: "", manager: "",
          startDate: "", endDate: "", status: "Active", createdAt: iso(new Date()),
          tasks:          structuredClone(seed.tasks),
          milestones:     structuredClone(seed.milestones),
          freeMilestones: structuredClone(seed.freeMilestones),
          dependencies:   structuredClone(seed.dependencies ?? [])
        }]
      };
      syncFromActiveProject();
      saveState();
      renderAll();
    });
 // Botão adicionar membro
  document.getElementById("addTeamMemberButton")
    ?.addEventListener("click", () => {
      const name = document.getElementById("teamMemberName").value;
      const role = document.getElementById("teamMemberRole").value;
      addTeamMember(name, role);
      document.getElementById("teamMemberName").value = "";
    });

  // Enter no campo nome também adiciona
  document.getElementById("teamMemberName")
    ?.addEventListener("keydown", e => {
      if (e.key === "Enter") {
        document.getElementById("addTeamMemberButton").click();
      }
    });

  // Delegação: remover membro via botão nos cards
  document.getElementById("teamCards")
    ?.addEventListener("click", e => {
      const btn = e.target.closest("[data-remove-member]");
      if (btn) removeTeamMember(btn.dataset.removeMember);
    });

  // Settings: usuários
  document.getElementById("addUserButton")
    ?.addEventListener("click", () => openUserModal());

  document.getElementById("closeUserModal")
    ?.addEventListener("click", () => document.getElementById("userModal").close());
  document.getElementById("cancelUserModal")
    ?.addEventListener("click", () => document.getElementById("userModal").close());
  document.getElementById("saveUserModal")
    ?.addEventListener("click", saveUser);

  // Enter no campo nome do userModal
  document.getElementById("userModalNome")
    ?.addEventListener("keydown", e => {
      if (e.key === "Enter") document.getElementById("saveUserModal").click();
    });

  // Delegação: editar / remover usuário
  document.getElementById("usersTable")
    ?.addEventListener("click", e => {
      const edit   = e.target.closest("[data-edit-user]");
      const remove = e.target.closest("[data-delete-user]");
      if (edit)   openUserModal(edit.dataset.editUser);
      if (remove) removeUser(remove.dataset.deleteUser);
    });

  // Settings: vínculos projeto-usuário
  document.getElementById("addProjectUserButton")
    ?.addEventListener("click", () => {
      const userId = document.getElementById("addProjectUserSelect").value;
      const funcao = document.getElementById("addProjectUserFuncao").value;
      addProjectUser(userId, funcao);
    });

  document.getElementById("projectUsersTable")
    ?.addEventListener("click", e => {
      const btn = e.target.closest("[data-remove-project-user]");
      if (btn) removeProjectUser(btn.dataset.removeProjectUser);
    });

     // ── Alerts drawer ──────────────────────────────────────────
  const alertsDrawer  = document.getElementById("alertsDrawer");
  const alertsOverlay = document.getElementById("alertsDrawerOverlay");

  function openAlertsDrawer() {
    alertsDrawer?.classList.add("open");
    alertsOverlay?.classList.add("open");
    document.body.style.overflow = "hidden";
  }

  function closeAlertsDrawer() {
    alertsDrawer?.classList.remove("open");
    alertsOverlay?.classList.remove("open");
    document.body.style.overflow = "";
  }

  document.getElementById("alertsButton")
    ?.addEventListener("click", openAlertsDrawer);

  document.getElementById("closeAlertsDrawer")
    ?.addEventListener("click", closeAlertsDrawer);

  alertsOverlay?.addEventListener("click", closeAlertsDrawer);

  // Fechar com Escape (listener separado do layoutDrawer)
  document.addEventListener("keydown", e => {
    if (e.key === "Escape" && alertsDrawer?.classList.contains("open")) {
      closeAlertsDrawer();
    }
  });


  // ── Export dropdown ────────────────────────────────────────
  const exportDropBtn  = document.getElementById("exportDropdownButton");
  const exportDropMenu = document.getElementById("exportDropdownMenu");
  const exportDropWrap = exportDropBtn?.closest(".export-dropdown-wrap");

  function openExportDropdown() {
    exportDropMenu.hidden = false;
    exportDropWrap?.classList.add("open");
  }

  function closeExportDropdown() {
    if (exportDropMenu) exportDropMenu.hidden = true;
    exportDropWrap?.classList.remove("open");
  }

  // Toggle ao clicar no botão
  exportDropBtn?.addEventListener("click", e => {
    e.stopPropagation(); // impede fechar imediatamente via document handler
    exportDropMenu?.hidden ? openExportDropdown() : closeExportDropdown();
  });

  // Fechar ao clicar em qualquer item do menu
  exportDropMenu?.addEventListener("click", closeExportDropdown);

  // Fechar ao clicar fora do dropdown
  document.addEventListener("click", e => {
    if (exportDropWrap && !exportDropWrap.contains(e.target)) {
      closeExportDropdown();
    }
  });

  // Fechar dropdown com Escape
  document.addEventListener("keydown", e => {
    if (e.key === "Escape" && !exportDropMenu?.hidden) {
      closeExportDropdown();
    }
  });

 document.addEventListener("focusin", e => {
    if (e.target.matches("input, select, textarea")) {
      e.target.classList.remove("input-error");
    }
  });


}

function toCsv(rows) {
  const headers = ["component", "description", "start", "end", "status", "progress", "milestone", "milestoneDate", "level"];
  return [headers.join(","), ...rows.map((row) => headers.map((key) => `"${String(row[key] ?? "").replaceAll('"', '""')}"`).join(","))].join("\n");
}

function renderTimeline() {
  const tasks = getFilteredTasks().sort((a, b) => a.component.localeCompare(b.component) || dateValue(a.start) - dateValue(b.start));
    if (!tasks.length) {
    $("#timeline").innerHTML = `
      <div class="view-empty-state">
        <i data-lucide="calendar-x-2"></i>
        <p>Nenhuma atividade encontrada</p>
        <small>${
          state.tasks.length
            ? "Ajuste os filtros para ver atividades."
            : "Adicione atividades na aba Activities."
        }</small>
      </div>`;
    lucide.createIcons();
    return;
  }
  const { min, max } = getRange(tasks.length ? tasks : state.tasks);
  const totalDays = Math.max(1, Math.round((max - min) / dayMs) + 1);
  const pxPerDay =  zoomConfig[zoom].pxPerDay;
  const timelineWidth = Math.max(680, Math.round(totalDays * pxPerDay));
  const months = getMonths(min, max);
  const todayOffset = ((new Date().setHours(0, 0, 0, 0) - min) / dayMs) * pxPerDay;
  const monthColumns = months
    .map((month, index) => {
      const next = new Date(month);
      next.setMonth(next.getMonth() + 1);
      const days = Math.max(1, Math.round((Math.min(next, max) - Math.max(month, min)) / dayMs));
      return `<div class="month-cell" style="width:${days * pxPerDay}px;grid-column:${index + 1}">${
        zoom === "quarter"
          ? quarterLabel(month)
          : monthLabel(month)
      }</div>`;
    })
    .join("");

  const rows = [];
  let currentComponent = "";
  const rowPositions = {};
  let currentOffset = 0;
  for (const task of tasks) {
    if (task.component !== currentComponent) {
      currentComponent = task.component;
    
      const collapsed = isComponentCollapsed(currentComponent);
      const icon = collapsed ? "▶" : "▼";

      const componentMilestoneMarkers = state.milestones
        .filter(m => m.component === currentComponent && m.start)
        .map((m, idx) => {
          const left = ((dateValue(m.start) - min) / dayMs) * pxPerDay;
          const colors = ["#4a5aa6", "#243782", "#b87900", "#c74444"];
          const color = colors[m.color] || colors[idx % colors.length];
          return `<span class="marker draggable-milestone" data-milestone-id="${m.id}" data-milestone-type="project" title="${escapeAttr(m.name)}" style="left:${left}px;top:11px;--marker:${color}"></span>
                  <span class="milestone-label" style="left:${left}px;top:-2px">${escapeHtml(m.name)}</span>`;
        }).join("");

      rows.push(`
        <div
          class="task-label component collapsible-component"
          data-component="${escapeAttr(currentComponent)}"
        >
          <span>${icon} ${escapeHtml(currentComponent)}</span>
        </div>
        <div
          class="lane component collapsible-component"
          data-component="${escapeAttr(currentComponent)}"
        >${componentMilestoneMarkers}</div>
      `);

      currentOffset += 38;
    }

    if (isComponentCollapsed(currentComponent)) {
      continue;
    }

    rowPositions[task.id] = {
      offset: currentOffset,
    };
    currentOffset += 44;
    rows.push(`<div class="task-label" style="padding-left:${14 + Number(task.level || 0) * 12}px">
      <strong>${escapeHtml(task.description || "Untitled")}</strong>
      <small>${formatDate(task.start)} - ${formatDate(task.end)}</small>
    </div>${renderTaskLane(task, min, pxPerDay, timelineWidth)}`);
  }

  const milestoneBand = `<div class="task-label">Project milestones</div><div class="milestone-band" style="width:${timelineWidth}px">${renderMilestones(min, pxPerDay)}</div>`;
  const dependencyLines = showDependencies
  ? renderDependencyLines(
      tasks,
      rowPositions,
      min,
      pxPerDay,
      timelineWidth
    )
  : "";
  $("#timeline").innerHTML = 
  //aqui
  `<div class="timeline-grid">
    ${dependencyLines}
    <div class="timeline-corner">${tasks.length} visible activities</div>
    <div class="months" style="grid-template-columns:repeat(${months.length}, auto);width:${timelineWidth}px">${monthColumns}</div>
    ${milestoneBand}
    ${rows.join("")}

  </div>${todayOffset >= 0 && todayOffset <= timelineWidth ? `<div class="today-line" style="left:${260 + todayOffset}px"></div>` : ""}`;
  $$(".lane").forEach((lane) => {
    lane.style.width = `${timelineWidth}px`;
    const gridSize = {
      week: 63,
      month: 126,
      quarter: 252
    };
    
    lane.style.backgroundSize =
      `${gridSize[zoom]}px 100%`;
  });
}

function refreshTimelineInteractions() {
  renderTimeline();
  initTaskDragging();
  initTaskResize();
  initMilestoneDragging();
}

function isTaskFieldLockedByDependency(taskId, field) {
  const dependencies = getTaskPredecessorDependencies(taskId);

  if (!dependencies.length) return false;

  return dependencies.some((dependency) => {

    if (["FS", "SS"].includes(dependency.type)) {
      return field === "start";
    }

    if (["FF", "SF"].includes(dependency.type)) {
      return field === "end";
    }

    return false;
  });
}

function download(filename, content, type) {
  const blob = new Blob([content], { type });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename;
  link.click();
  URL.revokeObjectURL(url);
}

function resolveAllColors(element) {
  const allElements = element.querySelectorAll("*");
  const modernColorFns = /(color-mix|color\(|oklch|oklab|lab\(|lch\(|hwb\()/i;

  const colorProps = [
    'color', 'background-color', 'border-color',
    'border-top-color', 'border-right-color',
    'border-bottom-color', 'border-left-color',
    'outline-color', 'text-decoration-color'
  ];

  allElements.forEach(el => {
    let style;
    try { style = getComputedStyle(el); } catch { return; }

    colorProps.forEach(prop => {
      const value = style.getPropertyValue(prop);
      if (!value || !modernColorFns.test(value)) return;

      const resolved = resolveColor(value);
      if (resolved) {
        try { el.style.setProperty(prop, resolved, 'important'); } catch {}
      } else {
        try { el.style.setProperty(prop, '#999', 'important'); } catch {}
      }
    });

    ['box-shadow', 'text-shadow'].forEach(prop => {
      const value = style.getPropertyValue(prop);
      if (value && modernColorFns.test(value)) {
        try { el.style.setProperty(prop, 'none', 'important'); } catch {}
      }
    });

    const filter = style.getPropertyValue('filter');
    if (filter && filter !== 'none') {
      try { el.style.setProperty('filter', 'none', 'important'); } catch {}
    }
  });
}

function resolveColor(val) {
  try {
    const canvas = document.createElement("canvas");
    canvas.width = 1;
    canvas.height = 1;
    const ctx = canvas.getContext("2d");
    
    ctx.fillStyle = val;
    ctx.fillRect(0, 0, 1, 1);
    
    const [r, g, b, a] = ctx.getImageData(0, 0, 1, 1).data;
    return a === 0 ? "transparent" : `rgb(${r},${g},${b})`;
  } catch (e) {
    console.warn("Failed to resolve color:", val);
    return null;
  }
}

/* ---------- estado do modal ---------- */
let _pngFullCanvas = null; // canvas gerado em escala 2x (base)
let _compactExportWidth = 0;
let _compactExportHeight = 0;

async function exportCompactTimelinePng() {
  const exportArea = document.getElementById("presentationExportArea");
  const dialog = document.getElementById("pngExportDialog");
  const canvas = document.getElementById("pngPreviewCanvas");
  const wrap = document.getElementById("pngPreviewWrap");
  const hint = document.getElementById("pngSizeHint");

  canvas.classList.remove("ready");
  wrap.classList.add("loading");
  hint.textContent = "Gerando preview de alta qualidade...";

  dialog.showModal();
  await new Promise(r => requestAnimationFrame(() => requestAnimationFrame(r)));

  try {
    const fullW = _compactExportWidth || exportArea.scrollWidth + 60;
    const fullH = _compactExportHeight || exportArea.scrollHeight + 120;

    const clone = exportArea.cloneNode(true);
    clone.style.cssText = `
      position:absolute; top:-99999px; left:-99999px; 
      width:${fullW}px; background:#ffffff; overflow:visible;
      font-family: Inter, system-ui, sans-serif;
      padding: 20px;
    `;

    // === APLICA MELHORIAS + RESOLVE CORES ===
    applyExportEnhancements(clone);
    resolveAllColors(clone);           // ← ESSA É A LINHA MAIS IMPORTANTE

    document.body.appendChild(clone);

    const quality = Number(document.getElementById("pngQualitySelect").value || 3);

    _pngFullCanvas = await html2canvas(clone, {
      scale: quality,
      backgroundColor: "#ffffff",
      useCORS: true,
      logging: false,
      width: fullW,
      height: fullH,
      windowWidth: fullW + 200,
      windowHeight: fullH + 200,
      imageTimeout: 0,
      removeContainer: true,
    });

 /*   const quality = Number(
  document.getElementById("pngQualitySelect").value || 3
);

const renderScale = Math.min(
  quality * window.devicePixelRatio,
  4
);
*/
    document.body.removeChild(clone);
    wrap.classList.remove("loading");
    canvas.classList.add("ready");

    autoFitPngScale();

  } catch (err) {
    console.error(err);
    showToast(`Erro ao gerar PNG: ${err?.message || err}`, "error");
    wrap.classList.remove("loading");
    dialog.close();
  }
}

function applyExportEnhancements(clone) {
  // Título
  if (document.getElementById("pngIncludeTitle")?.checked) {
    const title = document.createElement("div");
    title.style.cssText = `
      background: #0f172a; color: white; padding: 32px 40px 24px;
      font-size: 24px; font-weight: 800; text-align: center;
      border-bottom: 4px solid #1e40af; margin-bottom: 25px;
      border-radius: 8px 8px 0 0;
    `;
    title.innerHTML = `
      ${escapeHtml(state.projectName)}<br>
      <small style="font-size:14px; opacity:0.85">
        Timeline Compact • ${new Intl.DateTimeFormat('pt-BR', {dateStyle:'long'}).format(new Date())}
      </small>
    `;
    clone.prepend(title);
  }

  // Legenda
  if (!document.getElementById("pngIncludeLegend")?.checked) {
    clone.querySelectorAll("#compactLegend, .legend").forEach(el => el.remove());
  }

  // Melhorias visuais
  clone.querySelectorAll(".compact-component-label").forEach(el => {
    el.style.padding = "18px 22px";
    el.style.fontSize = "15.5px";
    el.style.borderBottom = "2px solid #cbd5e1";
  });

  clone.querySelectorAll(".compact-bar").forEach(bar => {
    bar.style.boxShadow = "0 4px 14px rgba(0,0,0,0.18)";
    bar.style.border = "1px solid rgba(0,0,0,0.15)";
  });
}
function enhancePngCanvas(sourceCanvas) {
  const margin = 60;
  const final = document.createElement("canvas");
  final.width = sourceCanvas.width + margin * 2;
  final.height = sourceCanvas.height + margin * 2;

  const ctx = final.getContext("2d");

  // Fundo branco com sombra suave
  ctx.shadowColor = "rgba(0, 0, 0, 0.12)";
  ctx.shadowBlur = 40;
  ctx.shadowOffsetY = 15;

  ctx.fillStyle = "#ffffff";
  ctx.fillRect(0, 0, final.width, final.height);

  ctx.shadowBlur = 0;
  ctx.shadowOffsetY = 0;
  ctx.drawImage(sourceCanvas, margin, margin);

  return final;
}


/* ---------- redimensiona o canvas de preview ---------- */
function applyPngScale(pct) {
  if (!_pngFullCanvas) return;
  const scale = pct / 100;
  const outW = Math.round(_pngFullCanvas.width * scale);
  const outH = Math.round(_pngFullCanvas.height * scale);

  const preview = document.getElementById("pngPreviewCanvas");
  preview.width = outW;
  preview.height = outH;

  const ctx = preview.getContext("2d");
  ctx.clearRect(0, 0, outW, outH);
  ctx.drawImage(_pngFullCanvas, 0, 0, outW, outH);

  document.getElementById("pngSizeHint").textContent = `${outW} × ${outH} px`;
}

/* ---------- auto-fit: encontra a escala que cabe na área ---------- */
function autoFitPngScale() {
  if (!_pngFullCanvas) return;
  const wrap = document.getElementById("pngPreviewWrap");
  const maxW = wrap.clientWidth - 40;
  const maxH = wrap.clientHeight - 40;

  const scaleW = maxW / _pngFullCanvas.width;
  const scaleH = maxH / _pngFullCanvas.height;
  const best = Math.min(scaleW, scaleH, 1.0);

  const pct = Math.max(20, Math.round(best * 100));
  document.getElementById("pngScaleSlider").value = pct;
  document.getElementById("pngScaleValue").textContent = `${pct}%`;
  applyPngScale(pct);
}

/* ---------- faz o download no tamanho escolhido ---------- */
function downloadPng() {
  if (!_pngFullCanvas) return;

  let canvasToDownload = _pngFullCanvas;

  // Aplica melhorias finais
  canvasToDownload = enhancePngCanvas(canvasToDownload);

  const link = document.createElement("a");
  link.download = `${state.projectName.replace(/[^a-z0-9]/gi, '_')}-timeline-compact.png`;
  link.href = canvasToDownload.toDataURL("image/png", 0.95);
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);

  document.getElementById("pngExportDialog").close();
  showToast("PNG exportado com sucesso!");
}

/* ---------- eventos do modal (adicionar dentro de bindEvents()) ---------- */
function bindPngDialogEvents() {
  document.getElementById("exportCompactPngButton")?.addEventListener("click", exportCompactTimelinePng);

  const scaleSlider = document.getElementById("pngScaleSlider");
  if (scaleSlider) {
    scaleSlider.addEventListener("input", () => {
      const pct = Number(scaleSlider.value);
      document.getElementById("pngScaleValue").textContent = `${pct}%`;
      applyPngScale(pct);
    });
  }

  document.getElementById("pngQualitySelect")?.addEventListener("change", () => {
    // Opcional: regenerar preview quando mudar qualidade
  });

  document.getElementById("pngFitBtn")?.addEventListener("click", autoFitPngScale); // caso ainda exista
  document.getElementById("pngPptBtn")?.addEventListener("click", fitToPptSlide);

  document.getElementById("confirmPngButton").addEventListener("click", downloadPng);
  document.getElementById("cancelPngButton").addEventListener("click", closePngDialog);
  document.getElementById("closePngDialog").addEventListener("click", closePngDialog);

  // Controles de layout compact (mantidos)
  document.getElementById("compactLabelWidth")?.addEventListener("input", e => {
    compactLabelColWidth = Number(e.target.value);
    document.getElementById("compactLabelWidthValue").textContent = `${compactLabelColWidth}px`;
    renderCompactTimeline();
  });

  document.getElementById("compactMonthWidth")?.addEventListener("input", e => {
    compactMonthScale = Number(e.target.value) / 100;
    document.getElementById("compactMonthWidthValue").textContent = `${e.target.value}%`;
    renderCompactTimeline();
  });
}

function closePngDialog() {
  document.getElementById("pngExportDialog").close();
  const canvas = document.getElementById("pngPreviewCanvas");
  canvas.classList.remove("ready");
  _pngFullCanvas = null;
}

// Converte "color(display-p3 r g b)" ou "color(srgb r g b)" para hex via Canvas 2D
function resolveColor(val) {
  // resolve color() e color-mix() para rgb() via canvas 1×1
  try {
    const c = document.createElement("canvas");
    c.width = c.height = 1;
    const ctx = c.getContext("2d");
    ctx.fillStyle = val;
    ctx.fillRect(0, 0, 1, 1);
    const [r, g, b, a] = ctx.getImageData(0, 0, 1, 1).data;
    return a === 0 ? "transparent" : `rgb(${r},${g},${b})`;
  } catch { return null; }
}

function fitToPptSlide() {
  if (!_pngFullCanvas) return;
  const targetW = 1500;
  const pct = Math.round((targetW / _pngFullCanvas.width) * 100);
  const clamped = Math.max(20, Math.min(200, pct));
  document.getElementById("pngScaleSlider").value = clamped;
  document.getElementById("pngScaleValue").textContent = `${clamped}%`;
  applyPngScale(clamped);
}

// mantém fallbackColor como alias para não quebrar nada
const fallbackColor = resolveColor;

// ── Importação Excel ────────────────────────────────────────────────────────

function formatDateDMY(isoDate) {
  if (!isoDate) return '';
  const [y, m, d] = isoDate.split('-');
  return `${d}/${m}/${y}`;
}

function exportExcelProject() {
  const wb = XLSX.utils.book_new();

  // ── Input Timeline ──────────────────────────────────────────
  const timelineRows = [
    [state.projectName || 'Projeto'],
    ['Component', 'Description', 'Initial Date', 'Final Date', 'Status', 'Status %',
     'Milestone Individual', 'Milestone Date', 'Milestone Status', 'Level'],
    ...state.tasks.map(t => [
      t.component, t.description,
      formatDateDMY(t.start), formatDateDMY(t.end),
      t.status || '',
      `${Math.round((t.progress || 0) * 100)}%`,
      t.milestone || '',
      formatDateDMY(t.milestoneDate),
      t.milestoneStatus || '',
      t.level ?? 0,
    ]),
  ];
  XLSX.utils.book_append_sheet(wb, XLSX.utils.aoa_to_sheet(timelineRows), 'Input Timeline');

  // ── Input Milestone ─────────────────────────────────────────
  const msRows = [
    ['Milestone', 'Type', 'Initial Date', 'Final Date', 'Level', 'Color Milestone'],
    ...state.milestones.map(m => [
      m.name, m.type,
      formatDateDMY(m.start), formatDateDMY(m.end),
      m.level ?? 0, m.color ?? 0,
    ]),
  ];
  XLSX.utils.book_append_sheet(wb, XLSX.utils.aoa_to_sheet(msRows), 'Input Milestone');

  // ── Input Milestone Livre ───────────────────────────────────
  const freeRows = [
    ['Milestone', 'Color', 'Initial Date', 'Componente', 'Nivel'],
    ...state.freeMilestones.map(fm => [
      fm.name, fm.status,
      formatDateDMY(fm.date),
      fm.component, fm.level ?? 0,
    ]),
  ];
  XLSX.utils.book_append_sheet(wb, XLSX.utils.aoa_to_sheet(freeRows), 'Input Milestone Livre');

  const fileName = `${(state.projectName || 'schedule').replace(/[^a-z0-9]/gi, '_')}.xlsx`;
  XLSX.writeFile(wb, fileName);
  showToast(`Excel exportado: ${fileName}`);
}

function parseBrDate(value) {
  if (!value) return '';
  if (value instanceof Date) {
    if (isNaN(value.getTime())) return '';
    const y = value.getUTCFullYear();
    const m = String(value.getUTCMonth() + 1).padStart(2, '0');
    const d = String(value.getUTCDate()).padStart(2, '0');
    return `${y}-${m}-${d}`;
  }
  const s = String(value).trim();
  const match = s.match(/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/);
  if (match) return `${match[3]}-${match[2].padStart(2, '0')}-${match[1].padStart(2, '0')}`;
  return '';
}

async function importExcelProject(file) {
  try {
    const data = await file.arrayBuffer();
    const wb = XLSX.read(data, { type: 'array', cellDates: true });

    // ── Input Timeline → tasks ──────────────────────────────────
    const sheetTimeline = wb.Sheets['Input Timeline'];
    if (!sheetTimeline) throw new Error('Aba "Input Timeline" não encontrada no arquivo.');

    const timelineRows = XLSX.utils.sheet_to_json(sheetTimeline, { header: 1, defval: '' });

    // Linha 0 (row 1 no Excel) = nome do projeto
    const projectName = String(timelineRows[0]?.[0] || 'Projeto Importado').trim();

    // Linha 1 = cabeçalhos — ignorar; linhas 2+ = dados
    const tasks = [];
    for (let i = 2; i < timelineRows.length; i++) {
      const row = timelineRows[i];
      const component   = String(row[0] || '').trim();
      const description = String(row[1] || '').trim();
      if (!component || !description) continue; // linha separadora ou vazia

      const start = parseBrDate(row[2]);
      const end   = parseBrDate(row[3]);
      if (!start || !end) continue;

      const status      = String(row[4] || '').trim();
      const progressVal = row[5];
      const progress    = Math.max(0, Math.min(1,
        typeof progressVal === 'number'
          ? (progressVal <= 1 ? progressVal : progressVal / 100)
          : (parseFloat(String(progressVal || '0').replace('%', '')) || 0) / 100
      ));
      const milestone       = String(row[6] || '').trim();
      const milestoneDate   = parseBrDate(row[7]);
      const milestoneStatus = String(row[8] || '').trim();
      const level           = parseInt(row[13] || 0, 10) || 0;

      tasks.push({
        id: crypto.randomUUID(),
        index: tasks.length + 1,
        component,
        description,
        start,
        end,
        status,
        progress,
        milestone,
        milestoneDate,
        milestoneStatus,
        level,
        baselines: [{ id: 'v1', start, end }],
      });
    }

    // ── Input Milestone → milestones ────────────────────────────
    const milestones = [];
    const sheetMs = wb.Sheets['Input Milestone'];
    if (sheetMs) {
      const msRows = XLSX.utils.sheet_to_json(sheetMs, { header: 1, defval: '' });
      for (let i = 1; i < msRows.length; i++) {
        const row  = msRows[i];
        const name = String(row[0] || '').trim();
        if (!name) continue;
        milestones.push({
          id:          crypto.randomUUID(),
          name,
          type:        String(row[1] || 'Milestone').trim() || 'Milestone',
          start:       parseBrDate(row[2]) || '',
          end:         parseBrDate(row[3]) || '',
          level:       parseInt(row[4] || 0, 10) || 0,
          color:       parseInt(row[5] || 0, 10) || 0,
          component:   '',
          description: '',
        });
      }
    }

    // ── Input Milestone Livre → freeMilestones ──────────────────
    const freeMilestones = [];
    const sheetFree = wb.Sheets['Input Milestone Livre'];
    if (sheetFree) {
      const freeRows = XLSX.utils.sheet_to_json(sheetFree, { header: 1, defval: '' });
      for (let i = 1; i < freeRows.length; i++) {
        const row  = freeRows[i];
        const name = String(row[0] || '').trim();
        const date = parseBrDate(row[2]);
        if (!name || !date) continue;
        freeMilestones.push({
          id:        crypto.randomUUID(),
          name,
          status:    String(row[1] || '').trim(),
          date,
          component: String(row[3] || '').trim(),
          level:     parseInt(row[4] || 0, 10) || 0,
        });
      }
    }

    // ── Cria novo projeto e ativa ───────────────────────────────
    commitState();
    syncToActiveProject();

    const newId = crypto.randomUUID();
    state.projects.push({
      id:            newId,
      name:          projectName,
      client:        '',
      manager:       '',
      startDate:     '',
      endDate:       '',
      status:        'Active',
      createdAt:     iso(new Date()),
      team:          [],
      tasks,
      milestones,
      freeMilestones,
      dependencies:  [],
    });

    state.activeProjectId = newId;
    syncFromActiveProject();
    saveState();
    renderAll();
    showToast(`"${projectName}" importado — ${tasks.length} atividades`);
  } catch (err) {
    showToast(`Erro ao importar: ${err.message}`, 'error');
    console.error(err);
  }
}

function escapeHtml(value) {
  return String(value ?? "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;");
}

function escapeAttr(value) {
  return escapeHtml(value).replaceAll('"', "&quot;");
}

let dragState = null;
let resizeState = null;
let milestoneDragState = null;
let compactDragState = null;
let compactMsDragState = null;


bindEvents();
renderAll();